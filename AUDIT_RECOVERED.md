
### FINDINGS lane (10)
- [high] CourseDetailScreen returns early before hooks (Rules of Hooks violation) | src/screens/study/CourseDetailScreen.tsx:48
    PROBLEM: `if (!courseId) return null;` is placed at line 48, BEFORE every useState (49-55) and useEffect (85-89). If the screen is ever navigated to without courseId (or React renders it once with no params), the early return runs a different number of hooks than the normal path, which throws "Rendered fewer hooks than expected" and crashes the screen. Conditional returns must come after all hook calls.
    FIX: Move the guard below all hook declarations: declare all useState/useCallback/useEffect first, then `if (!courseId) return null;` (or render an empty/error state) after them. Better: gate the data-loading inside the load callback (`if (!courseId) return;`) and never early-return before hooks.
- [high] Study Hub home never refreshes on focus — stale membership/courses/pins | src/screens/study/StudyHubScreen.tsx:664
    PROBLEM: `useEffect(() => { load(); }, [load])` runs only on mount. There is no `navigation.addListener('focus', ...)`. After a user joins by code, requests to create a section, an admin approves them, or they return from CourseDetail/StudyUpload (which adds courses/materials), the home screen still shows the old persona, old course list and old pins. CourseDetailScreen does subscribe to focus (line 86), so this screen is inconsistent and shows stale data until full remount.
    FIX: Add a focus listener mirroring CourseDetailScreen: `useEffect(() => { const unsub = navigation.addListener('focus', load); return unsub; }, [navigation, load]);` and re-run the sub-tab loaders (loadPins/loadJoinReqs/loadSecMembers) on focus too.
- [medium] Course rows always display "0 files" — aggregate count columns never selected | src/screens/study/StudyHubScreen.tsx:415
    PROBLEM: `load()` fetches courses with `supabase.from('study_courses').select('*')`. CourseRow (line 148) computes `total = material_count + question_count + book_count`, but `study_courses` rows from `select('*')` do not contain those aggregate fields (they come from materials/question_bank/books tables, not columns on study_courses). So `total` is always 0 and every course shows "0 files" even when it has materials.
    FIX: Either compute the counts via a view/RPC that returns material_count/question_count/book_count, or drop the file-count line. If counts are wanted, query the three child tables (count) per course or use a Postgres view and select those columns explicitly.
- [medium] User can request to join their own / already-joined section in Browse | src/screens/study/StudyHubScreen.tsx:972
    PROBLEM: renderBrowse lists all public sections and shows a "Request to join" button (line 1001-1008) with no check against mySection or existing membership. A member viewing Browse sees their own public section and can tap Request to join it again. requestJoin (724) blindly inserts a pending membership row; the result is either a duplicate pending request or a raw DB unique-constraint error surfaced to the user, and no "already a member" feedback.
    FIX: Filter out `mySection?.id` from the browse list, and disable/hide the join button when a membership row already exists. In requestJoin, detect a duplicate (error.code === '23505') and show a friendly "You already requested / are a member" toast instead of the raw error.message.
- [medium] flash() toast timeout not cleared on unmount — setState after unmount | src/screens/study/StudyHubScreen.tsx:366
    PROBLEM: `flash()` stores a setTimeout in toastRef and only clears the previous one on the next flash. There is no cleanup effect, so if the screen unmounts within 1.8s of a flash (e.g. user navigates away right after copying a code or submitting), the timer fires `setToastMsg('')` on an unmounted component (React warning / wasted work, potential leak in fast nav loops).
    FIX: Add `useEffect(() => () => { if (toastRef.current) clearTimeout(toastRef.current); }, []);` to clear the pending timer on unmount.
- [medium] toggleVisibility ignores DB error and desyncs optimistic state | src/screens/study/StudyHubScreen.tsx:744
    PROBLEM: toggleVisibility awaits the update but never checks `error`. It always optimistically sets `mySection.is_public = next` and flashes "Visibility updated". If RLS rejects the update (e.g. a non-CR somehow reaches it, or a transient failure), the UI shows the flipped state and a success toast while the DB is unchanged — silent desync that persists until reload.
    FIX: Capture `{ error }` from the update; on error show a toast and do NOT mutate local state (or revert). Only setMySection + flash on success.
- [low] approveJoin / setMemberRole / removeMember don't reconcile member<->joinReq lists | src/screens/study/StudyHubScreen.tsx:733
    PROBLEM: approveJoin updates status to approved and removes the row from joinReqs (740) but does not call loadSecMembers(), so the newly approved member does not appear in the MEMBERS list until the screen is reopened. The MEMBERS count badge and list are stale after an approval.
    FIX: After a successful approveJoin, also call loadSecMembers() (and ideally loadJoinReqs() to re-sync counts) so the approved member moves into the members list immediately.
- [low] loadStudents only loads first 100 profiles, no role filter — Assign CR search misses students | src/screens/study/StudyHubScreen.tsx:561
    PROBLEM: Assign-CR search reads `profiles` ordered by full_name limit 100 with no role filter and no server-side search; the client-side filter (line 1454) only searches within those first 100 alphabetical rows. On any campus with >100 profiles, most students are unreachable, and staff/admins are included in the candidate list for a student CR role.
    FIX: Filter by `role = 'student'`, and drive the search server-side: query with `.ilike('full_name', '%'+crSearch+'%').limit(30)` keyed off crSearch (debounced) instead of fetching a fixed 100 and filtering locally.
- [low] loadVote matches myBallot via cr_id === user.id with no membership/role guard | src/screens/study/StudyHubScreen.tsx:651
    PROBLEM: myBallot is derived from `ballots.find(b => b.cr_id === user?.id)`. The vote/ballot model is keyed on cr_id, but castVote and the vote box are shown in renderManage which is only reachable by CRs, yet there is no assertion the current user is the section CR before showing the Yes/No buttons. A demoted-but-still-cached persona, or a member who reaches manage state, would see vote controls; the RPC presumably rejects, but the UI offers an action it can't fulfill.
    FIX: Gate the cast-vote UI on `persona === 'cr'` (and re-verify role on focus). Keep relying on the RPC's server-side check, but don't render Yes/No for non-CRs.
- [low] StudyUpload course->section->intake lookup uses .single() and swallows errors | src/screens/study/StudyUploadScreen.tsx:65
    PROBLEM: The effect chains `.single()` twice (lines 65, 68) and discards errors. If courseId is invalid or RLS blocks the row, `.single()` errors and sectionId/intakeId stay null; canSubmit then silently stays false for questions/books with no message, so the Upload button is permanently disabled with no explanation. .single() (vs maybeSingle) also throws on 0 rows.
    FIX: Use maybeSingle(), capture errors, and surface a toast ("Couldn't load course") when the lookup fails so the disabled submit button isn't a silent dead-end.

### FINDINGS lane (9)
- [high] No error handling on any Supabase read; failed loads silently leave empty/stale UI with no error state | src/screens/faculty/FacultyScreen.tsx:35-47
    PROBLEM: load() destructures deptRes/facRes/savedRes and only acts on `.data` truthiness; the `.error` field on every query is ignored. If the faculty/departments query fails (RLS, network, etc.) the screen shows the empty branch list (or empty results) with no error UI and no way for the user to know it failed vs. genuinely empty. The same pattern repeats in FacultyDeptScreen.tsx:34-45 and FacultyProfileScreen.tsx:48-58. There is also no loading state on first mount in FacultyScreen/FacultyDeptScreen — before load() resolves the user sees an empty screen indistinguishable from 'no data'.
    FIX: Check `error` on each result and set an error flag; render an error/retry state. Add an initial `loading` state (true until first load resolves) so the empty list isn't shown prematurely. FacultyProfileScreen already has a loading spinner via `!member`, but it never distinguishes load failure from loading, so a failed fetch spins forever.
- [high] Bookmark toggle does optimistic state update with no error check or rollback; UNIQUE constraint makes double-tap fail silently | src/screens/faculty/FacultyScreen.tsx:57-69
    PROBLEM: toggleSave() updates local `savedIds` (setSavedIds(next)) regardless of whether the insert/delete succeeded — the awaited result's `.error` is discarded. The DB has UNIQUE (user_id, faculty_id) on faculty_bookmarks (verified). If the local state is stale (e.g. another device already bookmarked, or a rapid double-tap fires two inserts) the insert returns a duplicate-key error, but the star still flips on, leaving UI and DB out of sync. Conversely a failed delete leaves the star un-filled while the row still exists. Same bug in FacultyDeptScreen.tsx:55-67 and FacultyProfileScreen.tsx:60-68.
    FIX: Await the insert/delete, check `error`, and roll back the local state (restore previous Set / previous `saved` boolean) on failure. Use `.upsert(..., { onConflict: 'user_id,faculty_id', ignoreDuplicates: true })` for inserts to tolerate the unique constraint, or guard against concurrent toggles.
- [medium] No refresh-on-focus; bookmarks set on the profile screen don't sync back to list screens | src/screens/faculty/FacultyScreen.tsx:49
    PROBLEM: load() runs only once via useEffect on mount (deps are stable for a given user). The codebase establishes a refresh-on-focus convention with useFocusEffect (HomeScreen, NotificationsScreen, dashboards). Faculty screens don't use it. Flow: open directory -> open a teacher's FacultyProfile -> toggle the star there -> go back. FacultyScreen's savedIds is unchanged, so the star shows the old state and the 'Saved (n)' counter is wrong until a manual pull-to-refresh. FacultyDeptScreen.tsx:47 has the same staleness.
    FIX: Replace the mount-only useEffect with useFocusEffect(useCallback(() => { load(); }, [load])) so saved state re-syncs whenever the screen regains focus.
- [medium] facultyId can be undefined and is used in queries / rendered as the screen title without guard | src/screens/faculty/FacultyProfileScreen.tsx:44-58
    PROBLEM: facultyId = route.params?.facultyId ?? route.params?.id. If a navigation call passes neither (a broken nav param), facultyId is undefined; the effect's `if (!facultyId) return` prevents the query but member stays null forever, so the screen is stuck on the ActivityIndicator spinner with no error/empty fallback and no back affordance beyond SubBar. There's no user-facing indication the profile couldn't be resolved.
    FIX: When facultyId is missing, render an explicit 'Teacher not found' empty state instead of an indefinite spinner.
- [medium] deptId not validated; FacultyDeptScreen renders a blank header forever if param missing or dept fetch fails | src/screens/faculty/FacultyDeptScreen.tsx:27-47
    PROBLEM: deptId = route.params?.deptId with no guard. If undefined, the .eq('id', deptId).single() runs with an invalid filter and returns an error (ignored); dept stays null so the header block (line 87 `dept &&`) never renders and the SubBar shows the fallback title, while the roster query filters on `department_id = undefined` and returns nothing. The user sees an empty department with no error indication.
    FIX: Guard for missing deptId and surface an error/empty state; check deptRes.error and show a retry/not-found UI.
- [low] `qualifications` is jsonb (not a Postgres text[]) — non-array JSON values are silently dropped | src/screens/faculty/FacultyProfileScreen.tsx:80
    PROBLEM: The faculty.qualifications column is jsonb (verified), so a row could legitimately store a JSON object or string rather than an array. The code does `Array.isArray(member.qualifications) ? ... : []`, so any non-array jsonb value (object/string) renders as 'no qualifications' rather than displaying the data. The FacultyFull type also declares it as `string[] | null`, which misrepresents the jsonb column.
    FIX: Normalize jsonb qualifications: if it's a string wrap in [value]; if an object, extract the relevant field; only fall back to [] when truly empty. Update the type to reflect jsonb.
- [low] tel:/wa.me/mailto links opened via Linking.openURL with no error handling | src/screens/faculty/FacultyProfileScreen.tsx:170,187,195,214,230
    PROBLEM: Linking.openURL(...) is called directly (mailto, tel, wa.me, academic links, official profile). On a device without a mail/dialer/WhatsApp handler, openURL rejects with an unhandled promise rejection and the user gets no feedback. Same in facultyShared.tsx:159 (FacultyCard email link).
    FIX: Wrap in a helper that catches the rejection (and optionally canOpenURL-checks) and shows a toast/alert on failure.
- [low] research_interests pills use the interest string as React key — duplicate interests crash/warn | src/screens/faculty/FacultyProfileScreen.tsx:130-131
    PROBLEM: interests.map(i => <View key={i}>) keys by the raw interest text. If a faculty row has duplicate entries in the research_interests array (data is scraped per the comments), React throws a duplicate-key warning and pill reconciliation breaks. Same pattern in facultyShared.tsx:142-143 (slice(0,3)) and the popularInterests chips in FacultyScreen are keyed by it.label (FacultyScreen.tsx:191) which is deduped, so only the profile/card pill lists are exposed.
    FIX: Use a composite key like `${i}-${idx}` or dedupe interestsOf() output before rendering.
- [low] BRANCH_ORDER lists both 'Science / Social Sciences' and 'Social Sciences' as separate branches; departments with an unlisted branch fall to the end ungrouped | src/screens/faculty/facultyShared.tsx:34-50
    PROBLEM: BRANCH_ORDER and BRANCH_ICON enumerate fixed branch strings. branches grouping in FacultyScreen.tsx:97-107 appends any branch not in BRANCH_ORDER at the end, and BRANCH_ICON falls back to 'book-open' — fine — but the dual 'Science / Social Sciences' vs 'Social Sciences' entries suggest the branch taxonomy in code may not match DB values. If departments.branch is null, byBranch[null] groups under a literal 'null' header. No verification that code branch strings equal DB branch strings.
    FIX: Verify departments.branch distinct values against this list; coalesce null branch to an 'Other' bucket; collapse the redundant Science/Social Sciences entries.

### FINDINGS lane (9)
- [high] donors table has no `phone` column — DonorRegister phone is silently dropped from donor record; Donor type lies | src/types/database.ts:163
    PROBLEM: The `Donor` TS interface declares `phone: string | null`, but the live `donors` table has only (user_id, blood_group, area, last_donated, created_at, updated_at) — NO phone column. DonorRegisterScreen relies on this: it never writes phone into `donors` (it upserts only user_id/blood_group/area/last_donated at DonorRegisterScreen.tsx:41-46) and instead writes phone into profiles.whatsapp. The type is misleading and any code that reads `donor.phone` would always get undefined. Not a crash today but a latent data-model bug and a footgun for future code.
    FIX: Either remove `phone` from the `Donor` interface to match the actual table, or add a real `phone` column to donors. Make the type reflect the live schema.
- [medium] donor_contact RPC returns only { whatsapp } — Alert title `row.name` is always undefined | src/screens/blood/BloodScreen.tsx:85
    PROBLEM: revealContact does `Alert.alert(row?.name ?? t.blood2.contact, String(contact))`. The `donor_contact(p_user_id uuid)` RPC (verified in DB) returns `json_build_object('whatsapp', v_whatsapp)` only — there is no `name` key. So `row.name` is always undefined and the alert title silently falls back to the generic label, never showing the donor's name even though the UI already has `profiles.full_name` loaded for that donor.
    FIX: Pass the already-loaded donor name from the list into revealContact (e.g. revealContact(d.user_id, d.profiles?.full_name)) and use it as the alert title, or extend the RPC to also return name. Don't rely on a nonexistent row.name.
- [high] donor_contact RPC ignores profiles.show_whatsapp privacy flag — contact data leak | src/screens/blood/BloodScreen.tsx:78
    PROBLEM: The donor_contact RPC (SECURITY DEFINER) selects whatsapp from profiles by id with no check on the `show_whatsapp` boolean privacy flag (which exists on profiles and is used elsewhere in the app to gate contact reveal). Any signed-in user tapping Contact on any donor receives that donor's WhatsApp number regardless of whether the donor consented to share it. This contradicts the connections/directory privacy model noted for the app (contact only when consented).
    FIX: Update the donor_contact RPC to return whatsapp only when profiles.show_whatsapp is true (and ideally only to authenticated users), returning null/empty otherwise; the client already handles a null whatsapp via t.blood2.notShared.
- [medium] No refresh-on-focus: pledges/requests/donors go stale after navigating to BloodRequest/DonorRegister and back | src/screens/blood/BloodScreen.tsx:66
    PROBLEM: Data is loaded only via useEffect(()=>{load()},[load]) on mount. After the user posts a new request (BloodRequestScreen navigates goBack) or registers as a donor (DonorRegisterScreen goBack), BloodScreen is already mounted, so it does NOT reload — the new request/donor won't appear and counts are stale until manual pull-to-refresh. The rest of the app uses refresh-on-focus (per project memory, commit c663282).
    FIX: Use useFocusEffect(useCallback(()=>{ load(); },[load])) from @react-navigation/native so the list refreshes when the screen regains focus after posting.
- [medium] load() swallows all query errors — no error state, silent empty UI on failure | src/screens/blood/BloodScreen.tsx:55
    PROBLEM: load() destructures only `.data` from the three queries (rRes/dRes/pRes) and never inspects `.error`. If blood_requests/donors fail (RLS, network), the screen silently shows zero requests/donors with no error or empty-state messaging — indistinguishable from genuinely empty data. There is also no loading indicator on initial mount.
    FIX: Check each result's .error and surface a toast/error state; add an initial loading flag and an explicit empty-state message when arrays are empty after a successful load.
- [low] Missing empty states for requests and donors lists | src/screens/blood/BloodScreen.tsx:227
    PROBLEM: Both tabs render `requests.filter(...).map(...)` / `donors.filter(...).map(...)` directly. When there are no requests (or none match the selected blood-group filter), the requests tab renders a blank area with no message. Same for the donor list when a filter matches nothing. Users get a confusing empty screen.
    FIX: Render an empty-state component/text when the filtered array length is 0 (e.g. 'No requests for {group}').
- [low] Hardcoded English strings bypass i18n ('I can help', 'Post Request', 'Available Donors', 'Register as Donor') | src/screens/blood/BloodScreen.tsx:277
    PROBLEM: The app is EN/বাং via t.blood2.* dictionary, but several user-facing strings are hardcoded English literals: BloodScreen.tsx:277 'I can help' (the non-pledged button — note the pledged branch DOES use t.blood2.viewRequesterContact, so it's inconsistent), BloodScreen.tsx:289 'Available Donors', BloodRequestScreen.tsx:167 'Post Request', DonorRegisterScreen.tsx:128 'Register as Donor'. These won't translate to Bengali.
    FIX: Route all four through the t.blood2 dictionary like the surrounding strings.
- [low] Units input can be submitted as 0 or empty; parseInt fallback masks invalid input | src/screens/blood/BloodRequestScreen.tsx:101
    PROBLEM: The units TextInput strips non-digits, so the field can become empty string. canSubmit (line 38) does not validate units at all, and handleSubmit uses `parseInt(units,10) || 1`. So an empty units field silently posts 1 unit, and a user who clears it gets no feedback. There is also no upper bound. Minor data-quality issue.
    FIX: Validate units (>=1) as part of canSubmit and show the field as required, or default-display '1' and prevent clearing to empty.
- [low] lastDonated free-text date silently discarded if not exactly YYYY-MM-DD | src/screens/blood/DonorRegisterScreen.tsx:38
    PROBLEM: lastDonated is a free TextInput. handleSubmit only accepts it if it matches /^\d{4}-\d{2}-\d{2}$/, otherwise stores null with no warning. A user typing '12 June 2026' or '2026/06/12' has their input silently dropped and the donor shows as 'never donated' / 'eligible', which can be medically misleading on the donor list (eligibility pill at BloodScreen.tsx:307 keys off last_donated).
    FIX: Use a date picker, or validate/normalize the input and warn the user when the entered date can't be parsed instead of silently nulling it.

### FINDINGS lane (7)
- [high] Seller location rendered but column does not exist (always blank) | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketDetailScreen.tsx:156
    PROBLEM: The seller card subtitle renders `listing.location`, but the `listings` table has no `location` column (verified via information_schema: columns are id, code, title, price, condition, negotiable, category, description, photo_url, status, seller_id, created_at, updated_at). The `Listing` type in src/types/database.ts (lines 112-126) also has no `location` field. So `listing.location` is always undefined and the subtitle is permanently empty, leaving a blank line under the seller name. The `select('*')` never returns this field.
    FIX: Remove the location Text (or replace with a real field such as seller program/role fetched from profiles). If a location is genuinely wanted, add a `location` column to `listings` and to the post form/insert payload, and select it. Do not render a non-existent column.
- [high] No refresh-on-focus: list is stale after posting/editing/deleting/marking sold | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketScreen.tsx:60
    PROBLEM: MarketScreen loads listings only once on mount via `useEffect(() => { load(); }, [load])`. There is no `useFocusEffect`/navigation focus listener. After MarketPostScreen creates or edits a listing and calls `navigation.goBack()`, or after MarketDetailScreen marks Sold / deletes and goes back, the marketplace grid still shows the old data until the user manually pulls to refresh. The 'mine' tab and counts are also stale. Memory note project_campus_reports_feed/notifications established refresh-on-focus as the expected pattern (commit c663282).
    FIX: Replace the mount-only effect with `useFocusEffect(useCallback(() => { load(); }, [load]))` (from @react-navigation/native) so the grid reloads each time the screen regains focus.
- [medium] markSold has no error handling and optimistically desyncs on failure | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketDetailScreen.tsx:61
    PROBLEM: `markSold` fires `supabase.from('listings').update({ status: 'Sold' }).eq('id', listingId)` and ignores the returned error, then unconditionally sets local state to Sold. If the update is rejected (RLS denies a non-owner, network failure), the UI still shows Sold while the DB row is unchanged — an optimistic-update desync with no user feedback. The parent MarketScreen also won't reflect the change (see refresh-on-focus finding).
    FIX: Capture `{ error }`, show a toast on failure and do not mutate local state when error is non-null: `const { error } = await supabase...; if (error) { toast({type:'error',...}); return; } setListing(...)`.
- [medium] Contact reveal swallows RPC errors and shows misleading fallback | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketDetailScreen.tsx:53
    PROBLEM: `revealContact` destructures only `{ data: c }` from `supabase.rpc('listing_contact', { p_code: listing.code })`, ignoring the error. It then unconditionally sets `revealed=true`. On RPC failure (or when the seller has `show_whatsapp=false`, the RPC returns whatsapp=null by design), the UI shows the contact card with 'WhatsApp not shared' even though the call may have errored entirely, so the user can't tell a real failure from a withheld number. Also if `listing.code` were ever missing the RPC silently returns nothing.
    FIX: Destructure `{ data, error }`; on error show a toast and keep `revealed=false` so the user can retry. Only flip `revealed` on success.
- [low] price parsed with parseInt loses values and truncates; no max/empty guard documented | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketPostScreen.tsx:89
    PROBLEM: `price: parseInt(price, 10) || 0` means a price of 0 typed by the user, or a cleared field, silently posts as 0 (free). `price` column is a NOT NULL integer so 0 is accepted; combined with `canSubmit` only requiring `price.trim()` truthy, a user can post an item priced 0 by entering '0'. Minor, but there is no minimum-price validation and `|| 0` masks parse issues.
    FIX: Validate price > 0 in canSubmit (e.g. `Number(price) > 0`) and post `Number(price)` after validation rather than relying on `|| 0`.
- [low] Tab counts ignore active search/category filters (misleading), and 'all' count counts unfiltered list | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketScreen.tsx:104
    PROBLEM: The tab pill counts use the raw `listings.length` and `listings.filter(seller)` arrays, while the rendered grid is the filtered `list` (category + search applied). When a category or search query is active, the visible cells differ from the counts shown on the tab chips, which is confusing (chip says e.g. 60 while one item is shown).
    FIX: Either compute counts from the same filtered set used for rendering, or label the chips clearly as totals. At minimum apply the category/search filters to the count expressions for consistency.
- [low] Sold listings are still returned and shown in 'All'; no server-side status filter | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/market/MarketScreen.tsx:51
    PROBLEM: `load()` selects all listings ordered by created_at with no `.eq('status', ...)` filter, so Sold items appear in the grid (rendered with a Sold overlay). That may be intended, but combined with `.limit(60)` Sold items consume slots and can push available items out of the first 60 returned, so genuinely available listings can be hidden once enough Sold rows accumulate.
    FIX: If the product intent is a marketplace of available goods, filter `.eq('status','Available')` server-side (or order Available first), or raise/paginate beyond the hard 60 limit so Sold rows don't crowd out Available ones.

### FINDINGS lane (6)
- [high] Conditional early-return placed before hooks in DoctorDetailScreen violates Rules of Hooks | src/screens/medical/DoctorDetailScreen.tsx:58
    PROBLEM: `if (!doctorId) return null;` executes BEFORE `useState` (line 59) and `useEffect` (line 62). Returning before those hooks runs changes the hook call order between renders whenever `doctorId` toggles between falsy/truthy. React requires hooks to be called unconditionally in the same order every render; this throws "Rendered fewer hooks than expected" / "Rendered more hooks than during the previous render" and crashes the screen. Even though doctorId is currently always passed, the guard is unsafe as written.
    FIX: Move all hook calls to the top of the component, then guard after them: declare `useState`/`useEffect` first, and inside the effect bail out if `!doctorId`. Render the null/empty fallback only after hooks have been called (e.g. `if (!doctorId) return <fallback/>;` placed after the hook declarations, and make the effect a no-op when doctorId is missing).
- [medium] Medical screens never refresh on focus; cancel/advance/booking changes go stale | src/screens/medical/MyAppointmentsScreen.tsx:74
    PROBLEM: Data loads once via `useEffect(() => { load(); }, [load])` and only re-fetches on pull-to-refresh. The codebase establishes a refresh-on-focus pattern with useFocusEffect/navigation 'focus' listener in 7 other screens (HomeScreen, NotificationsScreen, dashboards, etc.). Without it, when a student cancels an appointment elsewhere, or returns from a flow that creates/changes an appointment, the list shows stale state until manual pull-to-refresh. Same defect in MedicalScreen.tsx:50 (doctor on-duty state) and MedicalQueueScreen.tsx:72 (admin returning to the queue sees yesterday's/stale rows).
    FIX: Reload on focus using `useFocusEffect(useCallback(() => { load(); }, [load]))` (from @react-navigation/native) in MyAppointmentsScreen, MedicalQueueScreen, and MedicalScreen, mirroring the existing pattern in HomeScreen.tsx/NotificationsScreen.tsx.
- [medium] load() queries silently swallow errors; no error state on any medical screen | src/screens/medical/MedicalScreen.tsx:45
    PROBLEM: Every fetch destructures only `{ data }` and ignores `error` (MedicalScreen.tsx:46, DoctorDetailScreen.tsx:64, MedicalQueueScreen.tsx:63, MyAppointmentsScreen.tsx:65). On a failed query `data` is null, state is left unchanged, and the user sees a permanent empty/loading state with no indication anything went wrong. DoctorDetailScreen is worst: on error `data` is null so `setDoctor` is never called and the screen is stuck on the ActivityIndicator (line 69-76) forever with no timeout or error path.
    FIX: Destructure `{ data, error }` and surface failures (toast or an inline error/retry state). For DoctorDetailScreen, track a `loading`/`error` flag so a failed fetch shows an error message instead of an infinite spinner.
- [medium] Admin role gate races against profile load; queue query runs for every role | src/screens/medical/MedicalQueueScreen.tsx:62
    PROBLEM: `load()` runs unconditionally in useEffect (line 72) for any user, and the access gate `if (profile && profile.role !== 'admin')` (line 89) only blocks rendering once `profile` is non-null. While `profile` is still null on first render a non-admin is not blocked, and the appointments query fires for them regardless of role. RLS (appts_select = student_id = auth.uid() OR is_admin()) prevents a data leak, but a non-admin student would briefly see their own appointments rendered in the admin queue UI before profile resolves, and the network call is wasted for non-admins.
    FIX: Guard the fetch on role: only call load() when `profile?.role === 'admin'`, and render a neutral loading state while `profile` is null instead of letting the query/render proceed.
- [low] isOnDuty assumes 24h hours and breaks for overnight shifts; bad time data passes invalidly | src/screens/medical/MedicalScreen.tsx:33
    PROBLEM: `isOnDuty` does `nowMins >= start && nowMins <= end`. If end_time is earlier than start_time (overnight shift) the doctor is always off-duty. Also `(doc.start_time ?? '').split(':').map(Number)` yields `[NaN]` for a non-empty but malformed time, and the destructuring defaults `[sh = 0, sm = 0]` only apply when the element is `undefined`, not `NaN` — so a malformed start_time produces `sh = NaN`, making the comparison always false (doctor silently never on duty). Same logic duplicated in DoctorDetailScreen.tsx:29.
    FIX: Validate parsed numbers with Number.isFinite and fall back to defaults on NaN; handle the overnight case (if end <= start, treat as wrapping past midnight) or document that shifts must be same-day.
- [low] fmtSlot crashes on null/undefined slot | src/screens/medical/MyAppointmentsScreen.tsx:42
    PROBLEM: `fmtSlot(hhmm)` calls `hhmm.split(':')` with no null guard (also MedicalQueueScreen.tsx:42). `slot` is NOT NULL in the DB so today it is safe, but the typed interface allows it and any embed/shape change yields a `Cannot read property 'split' of undefined` crash rendering the whole list. Defensive given lists map over it.
    FIX: Guard: `const [h = 0, m = 0] = (hhmm ?? '').split(':').map(Number);` consistent with how start_time/end_time are already defensively parsed elsewhere in the same files.

### FINDINGS lane (9)
- [high] DirectoryScreen does not refresh on focus — stale connection state after acting in StudentProfile | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:61
    PROBLEM: DirectoryScreen loads with `useEffect(() => { load(); }, [load])`, which runs only on mount. Directory and StudentProfile are sibling stack screens (AppNavigator.tsx:113-114). When a user taps a card, navigates to StudentProfile, accepts/declines/connects there, and presses back, the Directory list still shows the OLD connState (e.g. still shows a 'Connect' button or 'Requested' pill for someone now connected, or stale email contact). Every other data screen in the app (HomeScreen:126, NotificationsScreen:100, StaffDashboard:167, AdminDashboard:176) correctly uses `useFocusEffect`. This screen is the inconsistent outlier and produces visibly wrong UI.
    FIX: Import `useFocusEffect` from '@react-navigation/native' and replace the mount-only effect with `useFocusEffect(useCallback(() => { load(); }, [load]))` so the directory re-syncs whenever it regains focus.
- [medium] Redundant direct connections query can desync from the RPC status (multi-row tie-break differs) | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:42
    PROBLEM: The RPC `student_directory()` already returns an authoritative `status` per student, computed with `order by c.created_at desc limit 1` over the connections table (verified in DB). DirectoryScreen IGNORES that returned status (it spreads `...p` but never reads `p.status`) and instead re-derives connState from a second raw `connections` query using `(connections ?? []).forEach(...)` whose Map insertion is last-write-wins in arbitrary row order. The unique constraint is on (requester_id, addressee_id) so a pair CAN have two rows (one each direction). The RPC picks the newest; the client Map picks whichever row the query returned last. These can disagree, showing a different connState than StudentProfile (which DOES use the RPC status via STATUS_MAP). Also doubles the round-trips.
    FIX: Drop the second `connections` query entirely and map the RPC's own `status` field through a STATUS_MAP exactly like StudentProfileScreen.tsx:34-40 (accepted→connected, pending_outgoing→requested, pending_incoming→incoming). Single source of truth, no desync.
- [medium] RPC and connections-query errors are silently swallowed; no loading/error/empty states | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:40
    PROBLEM: `const { data: profiles } = await supabase.rpc('student_directory');` and the connections query both destructure only `data`, never `error`. If the RPC fails (e.g. the viewer is not a directory_visible student, in which case the RPC returns zero rows by design) the screen renders an empty white area with a search box and no message at all. There is no loading spinner (initial mount shows nothing until the awaits resolve) and no empty state for 'no students found' or 'directory unavailable'. A non-visible student or an admin (RPC reciprocity gate returns empty) sees a blank screen with no explanation.
    FIX: Capture `error` from the RPC, add `loading` and `error` state, and render an empty-state message when `filtered.length === 0` (distinguish 'no results for search', 'directory hidden for your account', and load error). StudentProfileScreen has the same swallow at line 53.
- [low] accept/decline mutations omit status='pending' filter — silent no-op on already-decided rows | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:78
    PROBLEM: The accept UPDATE (`.eq('requester_id', studentId).eq('addressee_id', user.id)`) and decline DELETE (line 83-84) match only on the id pair, not on status. The RLS USING clauses (`status='pending'` for both update and delete, verified in DB) protect the DB, but the practical effect is: if the row was already accepted/rejected by the time the tap lands (stale UI due to the refresh-on-focus bug above), the query affects 0 rows and returns NO error, so `if (error)` is false and the code optimistically sets connState anyway — e.g. decline on an already-accepted row leaves the DB accepted but the UI shows 'none'. Same pattern in StudentProfileScreen.tsx:72-78.
    FIX: Add `.eq('status','pending')` to the accept update and decline delete, then check the affected-row count (request `.select()` and verify a row came back) before applying the optimistic state; otherwise call load()/refresh().
- [low] No way to cancel an outgoing request; 'requested' pill is a dead end | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:141
    PROBLEM: When connState is 'requested' (outgoing pending), the UI shows only a static 'Requested' pill with no action. RLS connections_delete allows the requester to delete their own pending row (`requester_id = auth.uid()`), so cancellation is permitted by the backend, but neither DirectoryScreen nor StudentProfileScreen exposes it. A user who taps Connect by mistake can never retract it from the app.
    FIX: Make the 'requested' pill tappable to delete the outgoing pending row (`.delete().eq('requester_id', user.id).eq('addressee_id', s.id).eq('status','pending')`), then set connState back to 'none'.
- [low] Directory list rendered with .map inside ScrollView instead of FlatList; RPC returns unbounded rows | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:117
    PROBLEM: All students are rendered via `filtered.map(...)` inside a plain ScrollView. The `student_directory()` RPC has NO LIMIT (verified — it selects every directory_visible student ordered by full_name), so every visible student mounts at once with no virtualization. On a real campus this is a large list; all cards (with Avatars) render eagerly, hurting memory and scroll performance.
    FIX: Render with FlatList (keyExtractor={s => s.id}, the search box as ListHeaderComponent and an ListEmptyComponent) for virtualization, or add a LIMIT/pagination to the RPC.
- [low] StudentProfileScreen trusts navigation param shape with no null guard before first fetch | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/StudentProfileScreen.tsx:47
    PROBLEM: `const initial: DirectoryStudent = route.params?.student;` then `useState(initial)`. If the screen is ever navigated to without params (deep link, future caller), `initial` is undefined; `useState(undefined)` renders the early-return shell (line 86) which is fine, but `refresh()` keys on `initial?.id` and never resolves, so the screen is permanently a blank SubBar with no error/back affordance beyond the bar. The contract is only enforced by the loose `({ route, navigation }: any)` typing.
    FIX: In the `if (!student)` branch render an explicit empty/error message (e.g. 'Profile unavailable') rather than just an empty SubBar, and type the props with the StudentProfile route params from navigation.ts instead of `any`.
- [low] Dead/unsafe searchDirectory service: no role or reciprocity gating, unused by the directory screens | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/services/profileService.ts:31
    PROBLEM: `searchDirectory()` queries `profiles` directly with `.eq('directory_visible', true).ilike('full_name', ...)` and `select('*')`. It does NOT filter `role='student'` (would return visible staff/admin), does NOT enforce the reciprocity gate that the `student_directory` RPC enforces, and `select('*')` would pull every profile column including email/whatsapp regardless of connection state — a contact data leak IF this were called. The directory screens do not use it (they call the RPC and filter client-side), so it is dead code, but it is a latent leak if any caller adopts it.
    FIX: Delete searchDirectory, or reimplement it to call the `student_directory` RPC and filter client-side so it inherits the role gate, reciprocity gate, and connection-gated contact masking. Never `select('*')` from profiles for a public/peer search.
- [low] Hardcoded English 'Wants to connect' string bypasses i18n in DirectoryScreen | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:162
    PROBLEM: The incoming-request label is a literal `Wants to connect` string, while StudentProfileScreen.tsx:158 correctly uses `t.directory2.wantsToConnect`. The dictionary key exists in both en.ts:501 and bn.ts:502, so this is a missed i18n binding — Bangla users see English here even though the translation is available.
    FIX: Replace the literal with `{t.directory2.wantsToConnect}`.

### FINDINGS lane (9)
- [high] Events list does not refresh on focus — new/deleted/RSVP'd events stay stale | src/screens/events/EventsBrowseScreen.tsx:99
    PROBLEM: load() runs only once via useEffect([load]) on mount. After EventPostScreen creates an event and calls navigation.goBack() (EventPostScreen.tsx:95), or after EventDetailScreen deletes an event and goes back (EventDetailScreen.tsx:111), the browse list is not reloaded — the user returns to a stale list missing the new event or still showing the deleted one. There is no useFocusEffect / navigation 'focus' listener. Project memory notes refresh-on-focus was added to reports (commit c663282) for exactly this reason; events was missed.
    FIX: Replace the mount-only useEffect with useFocusEffect(useCallback(() => { load(); }, [load])) from @react-navigation/native, or add a navigation.addListener('focus', load) with cleanup in useEffect.
- [medium] EventDetailScreen does not refresh on focus and re-runs fetch on every auth-store change | src/screens/events/EventDetailScreen.tsx:51
    PROBLEM: Two issues. (1) The fetch effect has dependency array [id, user]; `user` is an object from the auth store, so any auth-store update (e.g. profile refresh) re-triggers the full fetch unnecessarily. (2) There is no refresh-on-focus, so the attending count / RSVP state shown can be stale relative to other users' RSVPs while the screen sits open or is returned to.
    FIX: Change the dependency to [id, user?.id]. Optionally wrap the fetch in useFocusEffect or re-fetch the head-count on focus so goingCount/capacity stay current.
- [medium] Capacity enforcement is client-side only — RSVP can exceed capacity (race) | src/screens/events/EventDetailScreen.tsx:77
    PROBLEM: isFull is derived purely from the locally-fetched goingCount (line 100) and only disables the button. The insert into event_rsvps (line 77) has no capacity guard, and goingCount comes from a count query taken at mount that is never re-validated. Two concurrent users (or one user on a stale count) can both pass the isFull check and insert, pushing attendance past event.capacity. The optimistic setGoingCount(c => c + 1) also never reconciles with the server, so the displayed count can drift from reality.
    FIX: Enforce capacity server-side (RLS policy or an RPC that checks count < capacity inside a transaction before inserting), and re-fetch the authoritative count after a successful insert/delete instead of relying solely on optimistic increment.
- [medium] Calendar view is hard-locked to the current month with no navigation | src/screens/events/EventsBrowseScreen.tsx:187
    PROBLEM: The calendar grid is built from `const now = new Date()` (year/month fixed to today). There are no prev/next month controls, so event-day dots and tap-to-list only ever work for the current calendar month. Any event in a past or future month (and the load() query pulls events ordered by date with no date filter, including other months) is invisible in calendar view — you cannot see or open them. Combined with the 'past' tab this is a real discoverability gap.
    FIX: Add month state (offset) with prev/next controls and compute year/month from it instead of `new Date()`.
- [low] No loading state on initial events fetch — shows false 'No events' empty state | src/screens/events/EventsBrowseScreen.tsx:89
    PROBLEM: EventsBrowseScreen tracks only `refreshing` (pull-to-refresh). On first mount while load() is in flight, events is [] so the list renders the 'No upcoming events' empty state (line 249-255) momentarily, then pops in the data. There is no loading spinner and no distinction between 'loading' and 'genuinely empty'.
    FIX: Add a `loading` state set true before the initial load and false after; render an ActivityIndicator while loading instead of the empty state.
- [low] Events fetch swallows errors silently (console.error only) | src/screens/events/EventsBrowseScreen.tsx:95
    PROBLEM: On query error load() only console.error's and returns; the UI shows the empty 'No events' state with no error indication or retry affordance. Same pattern in EventDetailScreen.tsx:58 where a failed event fetch just logs and returns, leaving the screen stuck on the infinite ActivityIndicator (line 88-95) with no error/empty/retry path.
    FIX: Surface a toast or an inline error+retry state on fetch failure; in EventDetail, set an error flag so the spinner is replaced with an error message instead of spinning forever.
- [low] capacity parsed with Number() can produce NaN on insert | src/screens/events/EventPostScreen.tsx:90
    PROBLEM: capacity: capacity.trim() ? Number(capacity.trim()) : null. keyboardType='number-pad' reduces but does not eliminate non-numeric input (paste, some IME/keyboards, decimal). Number('12a') / Number('1.5') yields NaN or a float; inserting NaN into the integer `capacity` column triggers a DB error that is caught generically (line 96-97) and reported only as the generic postEventError, with no field-level feedback.
    FIX: Use const capNum = parseInt(capacity.trim(), 10); and validate Number.isFinite(capNum) && capNum > 0 before insert, showing a field error otherwise; pass null when blank.
- [low] Event code generated from performance.now() — float value, collision-prone | src/screens/events/EventPostScreen.tsx:82
    PROBLEM: code: 'EVT-' + String(performance.now()) produces a fractional millisecond string like 'EVT-1234.5678901', which is an odd human-facing code and offers weak uniqueness (two posts in the same frame, or after a reload that resets the timer origin, can collide). Same pattern exists in AnnouncePostScreen.tsx:67. If the `code` column has a unique constraint, a collision surfaces only as the generic catch-all error.
    FIX: Generate a deterministic readable code server-side (DB default/sequence) or use a stable client id like Date.now() combined with a short random suffix, and ensure it is an integer string.
- [low] canPost in Browse and canCreate in Post use divergent organizer checks | src/screens/events/EventsBrowseScreen.tsx:71
    PROBLEM: EventsBrowseScreen gates the '+' button with admin || event_organizers row || club_members(president/vp) (lines 76-82). EventPostScreen recomputes its own gate (lines 50-54) with the same intent but as a separate query, so the two can disagree transiently (e.g. Browse sets isEventOrganizer=false default and only flips after the async resolves — if the user taps '+' is hidden, fine, but the duplicated logic risks drift). The real protection is the DB can_create_events() RLS, but the duplicated client checks are a maintenance hazard and can show the '+' to a user the Post screen then blocks (or vice-versa).
    FIX: Extract the organizer-permission check into one shared hook/util used by both screens so the UI gate cannot drift.

### FINDINGS lane (9)
- [high] Conditional early return placed before Hooks (Rules of Hooks violation -> crash) | src/screens/rides/RideDetailScreen.tsx:28
    PROBLEM: `if (!rideId) return null;` is executed BEFORE the component's useState calls (lines 29-34) and the useEffect (line 36). React requires hooks to be called unconditionally in the same order on every render. If `rideId` is ever falsy (e.g. navigated without params, or params cleared), the component returns before any hook runs, while on the previous render hooks did run. This produces 'Rendered fewer hooks than expected' and crashes the screen. Even if rideId is always set in practice, this is a latent defect.
    FIX: Move the guard below all hook declarations, e.g. compute everything with hooks first, then early-return inside the render body, or render a fallback: keep all useState/useEffect at the top and do `if (!rideId) { return <Fallback/>; }` only after the hooks are declared.
- [high] Rides list has no refresh-on-focus; new posts and request state go stale | src/screens/rides/RidesScreen.tsx:78
    PROBLEM: Data loads only via `useEffect(() => { load(); }, [load])`, which runs once on mount. RidePostScreen (RidePostScreen.tsx:122) and the delete actions in RideDetailScreen call `navigation.goBack()` and return to this list, but the list does not reload on focus. After offering a ride the user does not see it; after requesting/seeing changes elsewhere the seat counts and requestedIds are stale. The project convention (memory: commit c663282) is refresh-on-focus for feeds.
    FIX: Use `useFocusEffect(useCallback(() => { load(); }, [load]))` from @react-navigation/native instead of (or in addition to) the mount-only useEffect so the feed reloads each time the screen regains focus.
- [medium] RideDetail does not refresh on focus; requester list / contact / seat count go stale | src/screens/rides/RideDetailScreen.tsx:36
    PROBLEM: The detail screen fetches ride, own-request, requesters and taken count once in a mount useEffect keyed on [rideId, user?.id]. There is no refetch on focus and no re-run when seat requests change. A driver viewing the detail will not see new seat requests arrive, and a requester's seatsLeft/full state can be stale relative to other users. Combined with the list also not refreshing, optimistic increments are the only updates the user sees.
    FIX: Wrap the load logic in a callback and drive it with useFocusEffect so it re-runs on focus, or add a manual pull-to-refresh.
- [medium] ride_requests SELECT RLS is `qual: true` - any user can read every seat request | src/screens/rides/RideDetailScreen.tsx:50
    PROBLEM: The requesters query selects `requester_id, profiles:requester_id(full_name)` filtered only by `.eq('ride_id', rideId)`. The DB policy `ride_req_select` has qual=true, so the backend returns every ride's requester rows to any authenticated user. The screen only renders requesters when `driver_id === user?.id` (line 59), so the UI does not leak — but the data (who requested which ride, with names) is fully queryable by any client. This is a privacy leak at the data layer that the rides screens depend on.
    FIX: Tighten the RLS policy so a user can only SELECT ride_requests where they are the requester OR they own the ride: `qual: requester_id = auth.uid() OR EXISTS (SELECT 1 FROM rides r WHERE r.id = ride_id AND r.driver_id = auth.uid())`.
- [medium] Seat-count query in list ignores driver's own requests and lacks an explicit cap, causing wrong 'seats left' | src/screens/rides/RidesScreen.tsx:66
    PROBLEM: takenCounts is built by counting all ride_requests rows for each ride id. seatsLeft = seats_total - taken. This counts every request as a taken seat with no notion of accept/decline (there is no status column on ride_requests), so 'seats left' really means 'seats requested', which can mislabel a ride as Full purely from pending requests. Also the INSERT policy forbids a driver requesting their own ride, but the count makes no distinction — and there is no .limit, relying on PostgREST's implicit 1000-row default for the .in() count.
    FIX: Decide the intended semantics: if a request equals a taken seat, document it; otherwise add a status/accepted column and count only accepted requests. Either way the label text ('seats left') should match what is counted.
- [low] requestRide in list does not update takenCounts -> seat count desync vs detail screen | src/screens/rides/RidesScreen.tsx:86
    PROBLEM: After a successful insert (or 23505 dup), the list only adds the id to requestedIds; it never bumps takenCounts[rideId]. So the card's 'X seats left' stays at the pre-request value until a full reload, while RideDetailScreen.tsx:115 does optimistically increment takenCount. Behaviour is inconsistent between the two screens and the list under-reports taken seats until refresh.
    FIX: On a brand-new (non-dup) success, do `setTakenCounts(m => ({ ...m, [rideId]: (m[rideId] ?? 0) + 1 }))` to mirror the detail screen, distinguishing dup (error.code === '23505') from new insert.
- [low] Errors silently swallowed on ride request inserts | src/screens/rides/RidesScreen.tsx:89
    PROBLEM: `requestRide` checks `if (!error || error.code === '23505')` and otherwise does nothing — any real failure (RLS denial because the user is the driver, network error, etc.) is silently ignored with no toast/feedback. The user taps Request, nothing happens, and there is no indication why. (The detail-screen version at RideDetailScreen.tsx:122 does toast on error; the list version does not.)
    FIX: Add an else branch that surfaces the error via the toast/Alert, mirroring RideDetailScreen.requestRide.
- [low] Date validated for format only, not real calendar validity | src/screens/rides/RidePostScreen.tsx:102
    PROBLEM: The only date check is the regex `^\d{4}-\d{2}-\d{2}$`. Invalid dates like '2026-13-45' or '2026-02-30' pass the regex and are inserted into the DATE column. A past date also passes, so the ride is immediately stale (RidesScreen filters with `.gte('date', today)` so a past date silently never appears in the list, leaving the poster confused about why their ride is missing).
    FIX: Parse and validate the date (e.g. construct a Date and verify the round-trip matches, and reject dates before today), showing the existing invalidDate toast on failure.
- [low] load() ignores ride fetch errors; no error state on either screen | src/screens/rides/RidesScreen.tsx:57
    PROBLEM: load() only acts on `ridesRes.data`; if `ridesRes.error` is set (network/RLS), rides stays at its previous value (or empty) and the user sees the generic empty state with no error indication. RideDetailScreen.tsx:55 similarly only handles `rideRes.data` — on fetch error `ride` stays null and the screen shows an infinite ActivityIndicator (no timeout, no error message).
    FIX: Capture and surface fetch errors (toast or an inline error/retry state), and in RideDetail render an error/empty view instead of a permanent spinner when the ride fails to load.

### FINDINGS lane (9)
- [high] Bookmark toggle is optimistic but never checks the write result — UI/DB desync | src/screens/faculty/FacultyScreen.tsx:57
    PROBLEM: toggleSave updates savedIds state immediately, then fires supabase insert/delete without awaiting the {error} result or reconciling on failure. If the insert/delete fails (RLS denial, offline, network), the star stays toggled in the UI while the DB is unchanged. On the next load()/refresh the state silently reverts, confusing the user. Same pattern in FacultyDeptScreen.tsx:55 and FacultyProfileScreen.tsx:60. None of these surface an error.
    FIX: Capture the {error} from insert/delete; on error revert the optimistic Set change and show a toast/alert. e.g. const { error } = await supabase...; if (error) setSavedIds(prev => /* revert */).
- [medium] Bookmark insert can duplicate / race on rapid taps with no onConflict | src/screens/faculty/FacultyScreen.tsx:66
    PROBLEM: insert({ faculty_id, user_id }) has no .upsert/onConflict and toggleSave reads savedIds (closure) synchronously. Two fast taps before state settles can both branch to insert, producing a duplicate row (if no unique constraint) or a silently-swallowed unique-violation error (if a constraint exists). The faculty_bookmarks table has only id/user_id/faculty_id/created_at — no guarantee the swallowed error is benign. Same at FacultyDeptScreen.tsx:64 and FacultyProfileScreen.tsx:65.
    FIX: Use upsert with onConflict on (user_id, faculty_id) for the save path, and guard toggleSave against re-entry while a write is in flight.
- [high] No error handling on any data load — failures render as empty, not error | src/screens/faculty/FacultyScreen.tsx:35
    PROBLEM: load() destructures only .data from deptRes/facRes/savedRes and never inspects .error. A failed faculty/departments query (RLS, network) leaves departments/faculty empty and the screen shows a normal (non-error) UI as if there genuinely are no teachers. Identical in FacultyDeptScreen.tsx:34 and FacultyProfileScreen.tsx:51. There is no error state anywhere in the faculty domain.
    FIX: Read .error from each response; track an error flag in state and render an error/retry view distinct from the empty state.
- [high] FacultyProfile spins forever on invalid id or query error | src/screens/faculty/FacultyProfileScreen.tsx:48
    PROBLEM: The effect uses .single() and only sets member when memberRes.data is truthy. If facultyId is missing (effect returns early at line 49, member stays null) or the .single() query errors/returns no row, member remains null and the component renders the ActivityIndicator branch (line 70) indefinitely with no way out — no error or not-found state, and goBack is only reachable via the SubBar.
    FIX: Add a loading boolean separate from member; on error or no-row set a notFound state and render a 'Profile unavailable' message with the back action, rather than an endless spinner.
- [medium] Bookmarks saved on profile/dept screen don't refresh the directory (no refresh-on-focus) | src/screens/faculty/FacultyScreen.tsx:49
    PROBLEM: load() runs only in a useEffect on mount/[user.id]. Saving or unsaving a teacher on FacultyProfileScreen or FacultyDeptScreen mutates only that screen's local state; when the user navigates back to the directory, its savedIds and the saved-count badge (t.faculty2.savedLabel(savedIds.size)) are stale until a manual pull-to-refresh. The project pattern elsewhere (commit c663282) is refresh-on-focus; it is absent here.
    FIX: Use useFocusEffect(useCallback(() => { load(); }, [load])) so the directory re-pulls bookmarks when it regains focus.
- [low] Duplicate research-interest strings produce duplicate React keys | src/screens/faculty/FacultyProfileScreen.tsx:130
    PROBLEM: interests.map(i => <View key={i}>) keys list items by the interest string. research_interests is free-form data; if a faculty record has a repeated interest, React gets duplicate keys (warning + potential mis-reconciliation). Same in facultyShared.tsx:143 (interests.slice(0,3).map key={i}).
    FIX: Key by index or `${i}-${idx}` since these arrays are render-only and not reordered.
- [low] Silent truncation: departments .limit(50) and faculty .limit(1000) | src/screens/faculty/FacultyScreen.tsx:37
    PROBLEM: The directory caps departments at 50 (line 37) and faculty at 1000 (line 41). countByDept and branch grouping are computed from this truncated set, so beyond those limits department counts are wrong and teachers/departments silently vanish from the directory with no indication.
    FIX: Paginate or remove the hard caps (or at least raise them well above the real row counts and surface a 'showing N of M' note).
- [low] wa.me link breaks for local-format phone numbers | src/screens/faculty/FacultyProfileScreen.tsx:195
    PROBLEM: WhatsApp deep link is built as https://wa.me/${phoneDigits} where phoneDigits = phone.replace(/[^0-9]/g,''). For a Bangladeshi number stored as '01712...' this yields wa.me/01712... which WhatsApp interprets without a country code and fails to resolve to the right contact. wa.me requires full international format (8801712...).
    FIX: Normalize to international format before building the link (e.g. strip leading 0 and prepend 880, or store/require E.164 numbers).
- [low] FacultyDept renders empty roster with no error when deptId param is missing/invalid | src/screens/faculty/FacultyDeptScreen.tsx:36
    PROBLEM: deptId comes from route.params?.deptId with no guard. If navigated without the param, the department query runs .eq('id', undefined).single() (errors, dept stays null so the header is hidden) and the faculty query filters .eq('department_id', undefined) returning nothing; the screen shows the generic 'No teachers found' empty state with no header and no error, masking the real cause.
    FIX: Guard for a missing deptId and render an explicit error; check deptRes.error and show a load-failed state.

### FINDINGS lane (8)
- [high] Admin-only queue leaks all patients' data while profile is still loading (role-gate race) | src/screens/medical/MedicalQueueScreen.tsx:89
    PROBLEM: Gate is `if (profile && profile.role !== 'admin')`. While `profile` is null (auth store still hydrating), the guard is skipped and the full admin queue renders. Worse, `load()` in the mount useEffect (line 62-72) fires unconditionally regardless of role and selects every patient's full_name + avatar_url for today. A non-admin (or pre-hydration) user reaches this screen and the query runs; only RLS stands between them and every patient's PII. The UI gate is also defeated for the entire window where profile===null.
    FIX: Block render AND the fetch until role is known and confirmed admin: render an access-denied/loading state when `!profile || profile.role !== 'admin'`, and guard `load()` with `if (profile?.role !== 'admin') return;`. Also ensure server-side RLS restricts appointments SELECT to admins.
- [high] DoctorDetail shows an infinite spinner on fetch failure or missing doctor | src/screens/medical/DoctorDetailScreen.tsx:62-76
    PROBLEM: The effect does `const { data } = await supabase...maybeSingle(); if (data) setDoctor(...)`. The `error` is discarded, and `maybeSingle()` returns null data when the id doesn't exist or RLS blocks it. In both cases `doctor` stays null and the screen is stuck on `<ActivityIndicator>` forever (line 69-76) with no error or not-found state and no way out except Back.
    FIX: Capture `error`, track a `notFound`/`error` state, and render an explicit empty/error view (e.g. 'Doctor not found' + retry) when `data` is null after the fetch completes, instead of treating null as perpetual loading.
- [medium] Rules-of-Hooks violation: early return before hooks in DoctorDetailScreen | src/screens/medical/DoctorDetailScreen.tsx:57-59
    PROBLEM: `const { doctorId } = route.params ?? {}; if (!doctorId) return null;` executes BEFORE `useState`/`useEffect` (lines 59-67). React requires hooks to be called unconditionally in the same order every render. If this component ever re-renders with `doctorId` transitioning between present and absent (param change, navigation replace), hook order changes and React throws 'Rendered fewer hooks than expected', crashing the screen.
    FIX: Move the hooks above any conditional return; handle the missing-id case after hooks (e.g. render a fallback view), or guard the fetch inside the effect with `if (!doctorId) return;`.
- [medium] No refresh-on-focus: appointment/queue/doctor lists go stale | src/screens/medical/MyAppointmentsScreen.tsx:74
    PROBLEM: All four screens load only via `useEffect(() => { load(); }, [load])` on mount (MyAppointments:74, MedicalQueue:72, MedicalScreen:50, DoctorDetail:62). There is no `useFocusEffect`/navigation 'focus' listener (confirmed: none in the dir). After cancelling an appointment elsewhere, an admin advancing the queue, or booking on another screen, navigating back shows stale data until the user manually pulls to refresh. This contradicts the project's established refresh-on-focus pattern (commit c663282).
    FIX: Wrap `load` in `useFocusEffect(useCallback(() => { load(); }, [load]))` (from @react-navigation/native) on MyAppointments and MedicalQueue (and ideally MedicalScreen) so the data re-fetches each time the screen regains focus.
- [medium] Supabase errors silently swallowed in every load() | src/screens/medical/MedicalScreen.tsx:45-48
    PROBLEM: Every list fetch destructures only `{ data }` and ignores `error`: MedicalScreen:46, DoctorDetail:64, MedicalQueue:63-69, MyAppointments:65-71. On a network failure or RLS rejection the screen silently renders the empty state ('No doctors' / 'No appointments') as if there were genuinely no rows, with no error indication or retry, misleading the user.
    FIX: Destructure `error`, surface it (toast or inline error/retry state), and distinguish a real error from a genuinely empty result before showing the empty view.
- [low] No loading state on initial list fetch (false empty-state flash) | src/screens/medical/MedicalScreen.tsx:69-73
    PROBLEM: MedicalScreen and MyAppointments and MedicalQueue have no `loading` flag; `doctors`/`appts` start as `[]`, so on first paint before the fetch resolves they render the empty state ('No doctors available' / 'No appointments'). Users briefly see a misleading empty message on every entry. (DoctorDetail does show a spinner, but the lists do not.)
    FIX: Add a `loading` boolean initialized true, set false in load()'s finally, and render a spinner/skeleton instead of the empty state while loading is true and the list is empty.
- [low] fmtSlot / isOnDuty crash on null or malformed time strings | src/screens/medical/MedicalQueueScreen.tsx:42-47
    PROBLEM: `fmtSlot(hhmm)` does `hhmm.split(':')` with no null guard (also MyAppointments:42). `appointments.slot` and `doctors.start_time`/`end_time` are plain text columns; a null or empty value (or a value without ':') yields NaN-based output or, if null, throws 'cannot read split of null', crashing the row render. isOnDuty (MedicalScreen:33-34) guards with `?? ''` but fmtSlot does not.
    FIX: Guard fmtSlot against null/empty input (`if (!hhmm) return ''`) and validate the parsed hours/minutes are finite before formatting.
- [low] Optimistic queue advance can desync from server-derived token/state | src/screens/medical/MedicalQueueScreen.tsx:80-87
    PROBLEM: advance() optimistically maps local state to the next status after a bare `update({status}).eq('id')`. It does not re-select the row, so any server-side changes (e.g. a token assigned on Confirmed via trigger, or updated_at) are not reflected until a manual refresh. Combined with no refresh-on-focus, two admins advancing concurrently can show divergent state.
    FIX: After the update, either `.select().single()` and merge the returned row, or call load() to resync from the server.

### FINDINGS lane (8)
- [high] List never refreshes after posting a request or registering as a donor (stale state, no refresh-on-focus) | src/screens/blood/BloodScreen.tsx:66
    PROBLEM: Data is loaded once via useEffect(load) on mount only. BloodRequestScreen and DonorRegisterScreen both call navigation.goBack() to return here after a successful insert/upsert (BloodRequestScreen.tsx:54, DonorRegisterScreen.tsx:53). Because BloodScreen stays mounted under the navigator, returning to it does NOT re-run load(), so a newly posted request or newly registered donor does not appear, and the tab counts (requests.length/donors.length at lines 190-191) and the donor summary grid (lines 292-300) are stale until a manual pull-to-refresh. Other screens in this codebase (HomeScreen, AnnouncementsScreen, NotificationsScreen, etc.) already use useFocusEffect for exactly this reason.
    FIX: Replace useEffect(() => { load(); }, [load]) with useFocusEffect(useCallback(() => { load(); }, [load])) (from @react-navigation/native) so the feed reloads every time the screen regains focus after the child create screens pop back.
- [medium] load() swallows query errors and renders no error/empty state | src/screens/blood/BloodScreen.tsx:55
    PROBLEM: load() only reads rRes.data/dRes.data/pRes.data and ignores rRes.error/dRes.error/pRes.error (lines 61-63). On a failed/blocked query the screen silently shows an empty body with no toast, no error UI, and no empty-state placeholder for either the requests tab (lines 227-283) or the donors list (lines 304-341) — the user sees a blank screen and cannot tell whether there is genuinely no data or the fetch failed. There is also no loading indicator on initial mount.
    FIX: Capture and surface errors from each Promise.all result (toast on error), and render an explicit empty-state component when requests/donors are empty after load completes; add an initial loading flag.
- [low] respondedIds is loaded for ALL of a user's pledges but the visible request list is capped at 30 — view/pledge state can desync | src/screens/blood/BloodScreen.tsx:57
    PROBLEM: blood_requests is fetched with .limit(30) (line 57) while blood_pledges is fetched unfiltered for the user (line 59). respondedIds therefore reflects pledges to requests that may not be in the current 30-row window. This is benign for the visible list, but combined with the group filter (line 229) there is no pagination — once a campus has >30 open requests, older or filtered-out requests silently disappear with no 'load more', so a user can never reach or re-contact a requester for a request outside the newest 30.
    FIX: Either paginate blood_requests (infinite scroll / load more) or scope the limit per the active filter; at minimum document the 30-row cap and surface a 'showing latest 30' affordance.
- [low] Shared contactBusy flag disables every contact button at once | src/screens/blood/BloodScreen.tsx:53
    PROBLEM: A single contactBusy boolean gates revealContact (donor list, line 330/332) AND revealRequester (request cards, line 259/261). While any one contact reveal is in flight, every other donor's Contact button and every requester-contact button is disabled and shows '…' (lines 265, 335), even though they are unrelated rows. With a fast tap on one donor, all other rows visibly flicker to the busy state.
    FIX: Track the busy state per target id (e.g. const [busyId, setBusyId] = useState<string|null>) and disable/show '…' only on the row whose id matches.
- [medium] Hardcoded English strings bypass the i18n dictionary | src/screens/blood/BloodScreen.tsx:277
    PROBLEM: The app is EN/বাং via t.blood2.*, yet several user-facing labels are hardcoded English literals: 'I can help' (BloodScreen.tsx:277), 'Available Donors' (BloodScreen.tsx:289), 'Post Request' (BloodRequestScreen.tsx:167), and 'Register as Donor' (DonorRegisterScreen.tsx:128). In Bangla mode these stay English. Note the codebase even has a t.blood2.pledgedToHelp key, so the dictionary infrastructure is present and these were simply missed.
    FIX: Add the four strings to en.ts/bn.ts under blood2 and replace the literals with the t.blood2.* keys.
- [low] donor_contact / blood_requester_contact reveal a null WhatsApp as the contact value | src/screens/blood/BloodScreen.tsx:84
    PROBLEM: revealContact uses row?.whatsapp ?? t.blood2.notShared so a registered donor with no whatsapp on their profile shows 'not shared' — acceptable. But revealRequester (line 103) does Alert.alert(row.name, row.whatsapp ?? t.blood2.noWhatsapp): if the row exists but whatsapp is null it still opens an alert titled with the requester's real name and a 'no whatsapp' body, giving the donor no actionable contact while still exposing the name. More importantly there is no guard that whatsapp is non-empty before presenting it as a tappable/usable contact — the value is shown as plain text, not validated, so a malformed stored number is surfaced verbatim.
    FIX: When whatsapp is null/empty, show a single explanatory message (e.g. 'This requester has not shared a contact number') rather than the name+placeholder; consider validating/formatting the number before display.
- [low] units input can be submitted empty and area silently defaulted, hiding bad input | src/screens/blood/BloodRequestScreen.tsx:101
    PROBLEM: The units field strips non-digits (line 101) so the user can clear it to an empty string; canSubmit (line 38) does not check units, and handleSubmit does parseInt(units,10) || 1 (line 45), silently posting 1 unit when the field is blank. Separately, an empty area is silently replaced with 'Near campus' (line 49) even though area is a NOT NULL column with real meaning — the user gets no indication their location was discarded.
    FIX: Require units >= 1 in canSubmit and show the resolved value, and either make area a required field in canSubmit or clearly label that it defaults to 'Near campus' when left blank.
- [low] last_donated free-text input silently discards anything not exactly YYYY-MM-DD | src/screens/blood/DonorRegisterScreen.tsx:39
    PROBLEM: lastDonated is a plain TextInput (lines 112-118). On submit, /^\d{4}-\d{2}-\d{2}$/.test(ld) ? ld : null (line 39) silently coerces any non-matching entry (e.g. '2026/06/01', '01-06-2026', 'last month') to null with no validation feedback. The donor believes they recorded a donation date; the 90-day eligibility calc on the list side (BloodScreen.tsx:307) then treats them as 'Eligible' because last_donated is null. The regex also does not reject impossible dates like 2026-13-40.
    FIX: Use a date picker or validate the input on blur/submit and show an error toast when the format is invalid instead of silently nulling it; validate the date is real and not in the future.

### FINDINGS lane (9)
- [high] Events list missing refresh-on-focus; stale after post/delete | src/screens/events/EventsBrowseScreen.tsx:99
    PROBLEM: load() runs only in useEffect(..., [load]) which fires once on mount. EventPostScreen.handleSubmit and EventDetailScreen.deleteEvent both call navigation.goBack() to return here, but the browse list is never re-fetched. A newly created event does not appear, and a deleted event still shows, until the screen is remounted. The project explicitly adopted refresh-on-focus elsewhere (commit c663282) but it is absent here.
    FIX: Use useFocusEffect(useCallback(() => { load(); }, [load])) from @react-navigation/native instead of (or in addition to) the mount-only useEffect, so the list reloads when the screen regains focus.
- [high] events query truncates upcoming events via ascending order + limit(50) | src/screens/events/EventsBrowseScreen.tsx:90
    PROBLEM: .order('date', { ascending: true }).limit(50) returns the 50 chronologically EARLIEST events. Past events sort first, so once the table holds more than 50 rows the oldest past events fill the quota and genuinely upcoming events (the default/primary 'Upcoming' tab) get dropped entirely. The user sees a complete and incorrectly empty/partial Upcoming list.
    FIX: Either fetch upcoming and past with separate filtered queries (e.g. .gte('date', today) ordered ascending, and .lt('date', today) ordered descending), or order descending and paginate. Do not rely on a single ascending limit to populate a forward-looking list.
- [medium] No loading state: browse shows false 'No events' empty state during fetch | src/screens/events/EventsBrowseScreen.tsx:249
    PROBLEM: events initializes to [] and there is no loading flag. Between mount and load() resolving, list.length === 0 so the screen renders the 'No {filter} events' empty state. On a slow network the user briefly sees 'No upcoming events' even when events exist, which reads as a real empty result.
    FIX: Add a loading boolean (default true, set false in load()'s finally) and render a spinner/skeleton while loading instead of the empty state.
- [medium] Event code generated from performance.now() can collide with UNIQUE constraint | src/screens/events/EventPostScreen.tsx:82
    PROBLEM: code: 'EVT-' + String(performance.now()) is used for the events.code column, which has a UNIQUE constraint (events_code_key). performance.now() is relative to JS context start and resets to ~0 on every app launch, and produces non-integer values. Across app restarts / different devices, two events created at similar elapsed times can produce the same code, causing the insert to fail with a generic 'postEventError' toast and no diagnosis. It is also a fragile, non-collision-resistant identifier.
    FIX: Generate a collision-resistant code, e.g. crypto.randomUUID() based suffix or a DB-side default/sequence. Better: drop the client-generated code and give events.code a DB default (e.g. gen_random_uuid or a sequence).
- [medium] RSVP capacity enforced only client-side; over-capacity and duplicate RSVP races | src/screens/events/EventDetailScreen.tsx:77
    PROBLEM: isFull (line 100) is computed from goingCount fetched once on mount; capacity is never enforced in the DB. Two users (or one user across devices/stale screens) can RSVP past capacity because the count is stale and there is no server check. The PK (event_id,user_id) prevents true duplicates, but if 'going' state is stale=false the insert fails on the PK and the error is only console.logged (line 82) while goingCount is not corrected, leaving the UI desynced from the DB.
    FIX: Enforce capacity in DB (RLS/trigger or an RSVP RPC that checks count atomically). On insert error, surface a toast and re-fetch RSVP state/count rather than silently logging; treat unique-violation as 'already going' and set going=true.
- [medium] RSVP/event state never refreshed on focus; stale 'going' and count | src/screens/events/EventDetailScreen.tsx:51
    PROBLEM: The detail fetch effect runs on [id, user] only. goingCount and going can become stale if RSVPs change while the screen is backgrounded, and the optimistic count never reconciles with the server. Combined with the capacity gate this can show wrong attending counts and an incorrectly enabled/disabled RSVP button.
    FIX: Refetch the event + RSVP + count on focus (useFocusEffect), and re-read goingCount after a successful insert/delete instead of trusting the optimistic increment alone.
- [low] Detail effect depends on whole user object, risking refetch loops | src/screens/events/EventDetailScreen.tsx:63
    PROBLEM: useEffect dependency array is [id, user]. user is an object from the auth store; if its reference changes on unrelated store updates, the entire event/RSVP/count fetch re-runs unnecessarily (extra network, possible flicker).
    FIX: Depend on [id, user?.id] instead of the full user object.
- [low] Calendar view is locked to the current month with no navigation | src/screens/events/EventsBrowseScreen.tsx:187
    PROBLEM: The calendar grid is built from new Date() (year/month) with no prev/next controls. Events in any other month are invisible in calendar view, and dots only reflect the (already capped, see other finding) loaded events. Users cannot browse future-month events on the calendar.
    FIX: Add month navigation state (offset) and prev/next arrows; fetch/show events for the displayed month.
- [low] Browse 'isEventOrganizer' fetch has no error handling and ignores errors | src/screens/events/EventsBrowseScreen.tsx:79
    PROBLEM: Promise.all over event_organizers and club_members resolves without any .catch or error inspection; it only reads .data?.length. A network/RLS error leaves isEventOrganizer=false silently, hiding the post (+) button from a legitimate organizer with no feedback. (The query columns event_organizers.user_id and club_members.role/user_id are verified correct.)
    FIX: Inspect orgRes.error/clubRes.error, and add a .catch; optionally retry on focus so a transient failure doesn't permanently hide the post action for organizers.

### FINDINGS lane (7)
- [medium] MarketDetailScreen renders listing.location, a column that does not exist | src/screens/market/MarketDetailScreen.tsx:156
    PROBLEM: The seller card sub-line renders `{listing.location}`. The `listings` table has no `location` column (verified against the live DB: columns are id, code, title, price, condition, negotiable, category, description, photo_url, status, seller_id, created_at, updated_at), and the `Listing` type in src/types/database.ts:112-126 also has no `location` field. The detail query `select('*, profiles:seller_id(full_name)')` therefore never returns `location`, so this always evaluates to `undefined` and renders an empty Text. Dead/buggy code: the intended seller sub-line content is missing.
    FIX: Either remove the empty sub-line or replace it with a real value (e.g. category, condition, or a 'Seller' label / posted date). Do not reference `listing.location`.
- [high] MarketScreen does not refresh on focus; stale after post/edit/sold/delete | src/screens/market/MarketScreen.tsx:60
    PROBLEM: Data is loaded only via `useEffect(() => { load(); }, [load])` on mount plus pull-to-refresh. MarketPostScreen and MarketDetailScreen mutate listings (insert, update, markSold, delete) and then call `navigation.goBack()` to return to this screen, but there is no `useFocusEffect`/focus listener, so the grid shows stale data after creating, editing, marking sold, or deleting a listing until the user manually pulls to refresh. This codebase already uses refresh-on-focus elsewhere (HomeScreen, AdminDashboardScreen, StaffDashboardScreen, AnnouncementsScreen, NotificationsScreen).
    FIX: Wrap the load in `useFocusEffect(useCallback(() => { load(); }, [load]))` from @react-navigation/native (in addition to or instead of the mount effect) so the list refreshes whenever the screen regains focus.
- [high] markSold ignores errors and updates UI optimistically -> desync | src/screens/market/MarketDetailScreen.tsx:61
    PROBLEM: `markSold` runs `await supabase.from('listings').update({ status: 'Sold' }).eq('id', listingId)` with no error check, then unconditionally `setListing(prev => ({ ...prev, status: 'Sold' }))`. If the update fails (network, RLS, etc.) the detail screen still shows 'Sold', but the DB is unchanged. When the user navigates back, MarketScreen re-fetches from the DB and the item reappears as Available — an optimistic-update desync with no user feedback. There is also no toast on success/failure unlike deleteListing.
    FIX: Capture `{ error }`, show an error toast and return without mutating local state on failure (or refetch the row). Only set status to 'Sold' after a confirmed successful update.
- [medium] revealContact swallows RPC errors and reveals an empty contact card | src/screens/market/MarketDetailScreen.tsx:53
    PROBLEM: `revealContact` does `const { data: c } = await supabase.rpc('listing_contact', { p_code: listing.code })` discarding the `error`, then always calls `setRevealed(true)`. On any RPC failure (or when the row is missing), `c` is null, `contactInfo` stays null, and the UI flips to the revealed contact card showing the fallback 'WhatsApp not shared' / seller-name fallback — indistinguishable from a seller who genuinely opted out. The user gets no error indication and cannot retry (the Contact Seller button is gone after `revealed` is set).
    FIX: Destructure and check `error`; on error show a toast (t.common.error) and leave `revealed` false so the button remains. Only set `revealed` true when the RPC succeeded.
- [low] MarketScreen has no loading state; empty grid shown during initial fetch | src/screens/market/MarketScreen.tsx:48
    PROBLEM: Only a `refreshing` flag exists. During the initial `load()` (and if it returns no data due to an error, which is also silently swallowed at line 51-58 with no `error` check), the screen renders the empty state ('No items available' / 'No listings yet') which is indistinguishable from a real empty result or a failed fetch.
    FIX: Add a `loading` state initialized true, set false in load()'s finally; render a spinner while loading before falling back to the empty state. Also check the `{ error }` from the select and surface failures.
- [low] Edit payload re-sends seller_id on update | src/screens/market/MarketPostScreen.tsx:95
    PROBLEM: On edit, `handleSubmit` builds one payload that includes `seller_id: user.id` and uses it for both insert and update. For update this is unnecessary and, combined with the RLS UPDATE policy `(seller_id = auth.uid() OR is_admin())`, would let an admin editing another user's listing reassign ownership to the admin. In current UI only the owner sees the edit button, so it is not yet exploitable, but the field should not be in the update payload.
    FIX: Build separate payloads, or omit `seller_id` (and `status`) from the update branch so editing only touches the editable fields.
- [low] Tab count is inconsistent with the filtered/visible list | src/screens/market/MarketScreen.tsx:104
    PROBLEM: The 'all' chip count shows `listings.length` (the raw fetched array, capped at 60) and the 'mine' chip count shows `listings.filter(seller_id===user?.id).length`, neither of which account for the active category or search filters. The grid below is filtered by tab + category + search, so the badge number frequently disagrees with the number of cards shown.
    FIX: Either compute counts from the same filtered pipeline (excluding the tab filter), or label them clearly as totals. At minimum apply the category/search filters to the displayed counts for consistency.

### FINDINGS lane (8)
- [high] Failed profile fetch silently drops user into student UI (role-gating bypass) | src/store/authStore.ts:84-89
    PROBLEM: fetchProfile's error branch dispatches SET_PROFILE with profile:null, and the reducer (line 33) sets profileLoaded:true regardless. RootNavigator (RootNavigator.tsx:15) only blocks while session && !profileLoaded, so once profileLoaded flips true it renders AppNavigator with profile===null. BottomTabNavigator.tsx:41-44 picks HomeComponent via profile?.role, which falls through to HomeScreen for null. Net effect: any transient profile-load failure (network blip, RLS hiccup) for an admin or staff user silently logs them into the STUDENT UI with no error message and no retry path — they are stuck on the wrong dashboard until they kill and relaunch the app. There is also no on-focus or interval retry of fetchProfile anywhere.
    FIX: Distinguish 'profile failed to load' from 'no profile'. Either keep profileLoaded:false on error (so RootNavigator stays on the spinner) plus surface a retry, or add an explicit error state and render a 'Couldn't load your account — Retry' screen instead of defaulting to the student tabs. Do not treat a fetch error as a valid null profile.
- [medium] Concurrent duplicate profile fetches on cold start (last-write-wins race) | src/store/authStore.ts:61-76
    PROBLEM: On mount, getSession().then() calls fetchProfile (line 65) and, in parallel, onAuthStateChange fires INITIAL_SESSION and calls fetchProfile again (line 71) for the same user. Two identical requests race; whichever resolves last wins. If one errors and the other succeeds, ordering decides whether profile ends up valid or null, making finding #1 intermittent and order-dependent. There is also no in-flight guard / request token to discard stale responses (e.g. after a fast sign-out/sign-in).
    FIX: Guard fetchProfile with a request id or an isMounted/abort token, and avoid the double-trigger — e.g. rely solely on onAuthStateChange (which also fires INITIAL_SESSION) and drop the manual getSession().then(fetchProfile), or dedupe by userId already-loading.
- [medium] signOut not awaited / errors swallowed; UI can desync from real session | src/store/authStore.ts:110-113
    PROBLEM: signOut() calls supabase.auth.signOut() then optimistically dispatches SIGN_OUT. If signOut() rejects (network), the error is unhandled and the local state is wiped anyway, so the UI shows the logged-out (Auth) stack while the Supabase session/token may still be valid on disk — and the subsequent onAuthStateChange may re-hydrate the session, bouncing the user back in. The optimistic SIGN_OUT dispatch is also redundant with the onAuthStateChange(SIGNED_OUT) handler.
    FIX: Wrap in try/catch, surface failures to the caller, and let the onAuthStateChange SIGNED_OUT handler be the single source that clears state, instead of an unconditional optimistic dispatch.
- [medium] Forgot-password reset email sent with no redirect/deep-link config | src/screens/auth/LoginScreen.tsx:43
    PROBLEM: supabase.auth.resetPasswordForEmail(addr) is called with no options.redirectTo. In a mobile app with no web reset page, the default email link points at the Supabase/site URL, not back into the app, so the user has no in-app way to actually set a new password — the 'Reset link sent' confirmation (resetLinkSent) is shown but the flow dead-ends. There is also no email-format validation here (unlike Register), so a malformed string is sent straight to the API.
    FIX: Pass options.redirectTo with the app deep link and implement the corresponding update-password screen, or remove the feature until the redirect/deep-link is wired. Validate the email format before calling the API (reuse the Register regex).
- [low] Forgot-password button not disabled by ok/email state; uses busy only | src/screens/auth/LoginScreen.tsx:35-54,128
    PROBLEM: handleForgotPassword reads email at call time and shows forgotPasswordHint when empty, but the Forgot link is only disabled={busy}; it is tappable with an empty/garbage email. Combined with finding #4 (no format validation), tapping with junk fires a network request. Minor UX/abuse surface.
    FIX: Validate email format and short-circuit before the API call; optionally disable the link when email.trim() is empty.
- [low] setState after unmount in Register/Login submit handlers | src/screens/auth/RegisterScreen.tsx:57-63
    PROBLEM: On successful signUp (and signIn in Login), Supabase auto-confirm triggers onAuthStateChange, RootNavigator swaps to AppNavigator, and this screen unmounts. The finally block then runs setBusy(false)/busyRef on the unmounted component (LoginScreen.tsx:66-68 has the same pattern). Not a crash, but produces React 'state update on unmounted component' warnings and is fragile.
    FIX: Track an isMounted ref (set false in a useEffect cleanup) and skip the finally state updates when unmounted, or move the busy reset before the navigation-causing await resolves.
- [low] email_is_registered failure is silently ignored, defeating the duplicate-email guard | src/screens/auth/RegisterScreen.tsx:52-56
    PROBLEM: const { data: taken } = await supabase.rpc('email_is_registered', ...) destructures only data and ignores error. If the RPC call errors (network/permissions), taken is undefined, the guard is skipped, and the comment's own admission that Supabase's duplicate response is 'unreliable' under auto-confirm means a duplicate signup can proceed without the intended hard block — with no error shown to the user.
    FIX: Capture error from the rpc call; on error, either abort with a generic registerFailed message or fall back explicitly, rather than silently treating undefined as 'not taken'.
- [low] PasswordInput spreads style after defaults, letting callers override secureTextEntry-related layout but autoCapitalize is overridable | src/components/ui/PasswordInput.tsx:23-28
    PROBLEM: autoCapitalize="none" is set before {...rest}, so a caller passing autoCapitalize would override it; more importantly the Props type omits 'secureTextEntry' but nothing prevents a caller passing autoCorrect/textContentType that conflict with password handling. Low impact today (both auth screens pass safe props), but the default autoCapitalize can be clobbered because it precedes the spread.
    FIX: Put hard defaults (autoCapitalize, autoCorrect, textContentType="password"/oneTimeCode) AFTER {...rest} so they cannot be accidentally overridden by callers.

### FINDINGS lane (6)
- [high] Connection state never refreshes on screen focus — stale list/profile after accept/connect | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:61
    PROBLEM: Both screens load once via useEffect(() => { load() }, [load]) and never re-run on focus. Flow: open Directory (B shows as 'incoming'), tap into StudentProfileScreen, accept the request there (StudentProfileScreen state -> 'connected'), navigate back. DirectoryScreen state is untouched, so the list still shows the old 'incoming' Accept/Decline buttons and no contact email. Pressing Accept again then hits RLS connections_update (USING status='pending') which now fails because the row is already 'accepted', and load() resets it — confusing double action. The project already standardised refresh-on-focus (memory: commit c663282) and this domain does not use it.
    FIX: Replace the mount-only useEffect with useFocusEffect(useCallback(() => { load(); }, [load])) (from @react-navigation/native) in DirectoryScreen, and the same with refresh in StudentProfileScreen, so returning to either screen re-syncs connection state and contact details.
- [medium] DirectoryScreen ignores the RPC's authoritative status and re-derives it from a second, desync-prone connections query | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:40
    PROBLEM: student_directory() already returns a per-row `status` column (none/pending_outgoing/pending_incoming/accepted) plus email/whatsapp gated by acceptance. DirectoryScreen discards that status and instead issues a second query against `connections` (lines 42-53) to rebuild connState. The two sources are fetched at different instants and via different paths (RPC = SECURITY DEFINER, direct query = RLS connections_select), so they can disagree: e.g. the RPC may return email!=null (accepted) while the connections-derived map says 'requested', or vice versa, producing a card whose pill and contact block contradict each other. It is also a redundant round-trip. StudentProfileScreen does it correctly via STATUS_MAP[row.status].
    FIX: Drop the separate connections query in DirectoryScreen.load and map connState from the RPC row using the same STATUS_MAP that StudentProfileScreen uses: connState: STATUS_MAP[p.status] ?? 'none'.
- [medium] No error / loading / empty states; RPC and query errors are silently swallowed | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:40
    PROBLEM: load() destructures only `data` from supabase.rpc('student_directory') and from the connections query, ignoring `error`. student_directory() returns zero rows for any non-student or non-visible caller (the reciprocity gate `return;`), and returns nothing on error. The result is an indistinguishable blank screen for: (a) genuine errors, (b) admins/staff who legitimately get no rows, (c) a student who has toggled directory_visible off. There is no loading spinner on first mount and no empty-state message anywhere in the JSX (lines 116-197 just render an empty list). Users get a silent blank scroll view.
    FIX: Capture { data, error } from the RPC; track loading/error in state. Render a loading indicator on first load, an error message + retry on error, and an explicit empty-state (e.g. 'Directory is available to students with a visible profile') when filtered.length === 0.
- [low] Hardcoded English 'Wants to connect' string bypasses i18n dictionary | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:162
    PROBLEM: The incoming-request label is the literal string "Wants to connect" instead of t.directory2.wantsToConnect. The key exists and is correctly localized (en.ts:501 / bn.ts:502 'সংযুক্ত হতে চায়') and StudentProfileScreen.tsx:158 already uses it. In Bangla mode this card renders untranslated English.
    FIX: Replace the literal with {t.directory2.wantsToConnect}.
- [low] searchDirectory() bypasses the role and reciprocity gates the RPC enforces (latent data leak) | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/services/profileService.ts:31
    PROBLEM: searchDirectory selects '*' from profiles filtered only by directory_visible=true and full_name ilike. It does NOT filter role='student' and does NOT enforce the caller-is-visible-student reciprocity gate that student_directory() enforces, and it returns full profile rows ('*' includes email/whatsapp/etc.) rather than the acceptance-gated subset. It is currently dead code (no callers — grep finds only the definition), but if wired into the directory UI it would expose staff/admin profiles and contact fields to anyone, defeating the privacy model documented at the top of StudentProfileScreen.
    FIX: Either delete searchDirectory, or route it through the student_directory() RPC with a server-side name filter so the role gate, reciprocity gate, and accepted-only contact masking are preserved. Never select '*' for directory results.
- [low] Outgoing connection requests can never be withdrawn from the UI | c:/Users/Administrator/Desktop/CampusOne/CampusOne/src/screens/directory/DirectoryScreen.tsx:141
    PROBLEM: When connState==='requested' (outgoing pending) both screens render only a static 'Requested' pill with no action (DirectoryScreen 141-146, StudentProfileScreen 135-142). The connections_delete RLS policy explicitly permits the requester to delete a pending row ((status='pending') AND requester_id=auth.uid()), so a cancel action is allowed by the backend but is not exposed. A user who taps Connect by mistake is stuck in 'requested' forever (the unique index (requester_id,addressee_id) also blocks re-inserting).
    FIX: Add a cancel/withdraw action on the 'requested' state that runs supabase.from('connections').delete().eq('requester_id', user.id).eq('addressee_id', studentId), then re-sync.

### FINDINGS lane (8)
- [high] Course file counts always render 0 — count columns don't exist on study_courses | src/screens/study/StudyHubScreen.tsx:415-418
    PROBLEM: load() does `supabase.from('study_courses').select('*')` and the Course interface declares material_count/question_count/book_count, but the study_courses table has only id, section_id, code, name, created_by, created_at, updated_at (verified against the live DB). Those count columns are never returned, so CourseRow's `total = (c.material_count ?? 0) + (c.question_count ?? 0) + (c.book_count ?? 0)` is always 0 and every course row shows "0 files" regardless of how many materials/questions/books exist.
    FIX: Either compute counts via aggregate queries / a view / RPC (e.g. count rows in study_materials+study_question_bank+study_books per course_id) and merge them into each Course, or drop the misleading '{total} files' label. Selecting '*' will never populate these client-side fields.
- [medium] StudyHub home does not refresh on screen focus — stale courses/pins/membership | src/screens/study/StudyHubScreen.tsx:664
    PROBLEM: The only initial-load trigger is `useEffect(() => { load(); }, [load])`. There is no navigation 'focus' listener (CourseDetailScreen.tsx:86 has one; this screen does not). After a user uploads a file in StudyUpload or returns from CourseDetail, or after a join request is approved on another device, returning to StudyHub shows stale data (course list, pins, membership/persona) until the app is reloaded. The memory notes a project-wide refresh-on-focus pattern that is missing here.
    FIX: Register a focus listener mirroring CourseDetailScreen: `useEffect(() => { const unsub = navigation.addListener('focus', load); return unsub; }, [navigation, load]);` (and re-run sub-specific loaders like loadPins/loadSecMembers as appropriate).
- [low] flash() toast timeout not cleared on unmount — setState after unmount / leak | src/screens/study/StudyHubScreen.tsx:364-370
    PROBLEM: flash() stores a setTimeout in toastRef and clears the previous one on the next flash, but there is no cleanup effect that clears toastRef.current when the component unmounts. If the user navigates away within the 1800ms window, the timer fires setToastMsg('') on an unmounted component (React warning / wasted work). The ref is created but never torn down.
    FIX: Add `useEffect(() => () => { if (toastRef.current) clearTimeout(toastRef.current); }, []);`.
- [medium] Public-section join codes are fetched to every browsing user | src/screens/study/StudyHubScreen.tsx:457-465
    PROBLEM: loadPublicSections() selects `join_code` for every is_public section and mapSection() keeps it on each SectionRow handed to the Browse list. Any student browsing public sections receives the private join codes of sections they are not a member of. The browse flow only needs to request-to-join (requestJoin), so the join_code is never used in renderBrowse — it is leaked needlessly. Whether it is fully exploitable depends on RLS, but the client query explicitly pulls a secret it doesn't need.
    FIX: Drop join_code from the loadPublicSections select list (select only id, intake_id, number, is_public + the intake/department join). Keep join_code reads restricted to the member/manage path where mySection is the caller's own section.
- [low] requestJoin surfaces raw Postgres 23505 on duplicate / prior request | src/screens/study/StudyHubScreen.tsx:724-731
    PROBLEM: requestJoin() inserts into study_section_members with the UNIQUE(section_id,user_id) constraint live in the DB. If the user already has a row for that section (a still-pending request, a prior membership, or a previously rejected/removed-then-re-requested state) the insert fails with code 23505 and the user sees a raw 'duplicate key value violates unique constraint ...' message. There is also no optimistic UI feedback that the request is already pending.
    FIX: Detect error.code === '23505' and show a friendly message (e.g. 'You already requested to join this section'), and/or pre-check for an existing row. Other inserts in this file already special-case 23505 (addIntake:528, addCatSection:541) — apply the same here.
- [low] loadVote/load both fire on focus->sub change and can race the persona/visibility flip | src/screens/study/StudyHubScreen.tsx:798-808
    PROBLEM: castVote() calls loadVote(mySection.intake_id) and then load() back-to-back. load() re-reads membership and may reset mySection (and thus persona) while loadVote is still resolving against the old mySection.intake_id. There is no cancellation/guard, so on a vote that closes and flips intake visibility the two async refreshes can interleave and momentarily show inconsistent vote/visibility state. Same unguarded pattern exists wherever load() is called after a mutation.
    FIX: Sequence the refreshes (await load() then loadVote with the fresh intake id) or guard loadVote against a stale intake id (ignore the result if mySection.intake_id changed). Minor, but it causes flicker/inconsistent tallies right at vote close.
- [low] CourseDetail renders nothing (null) when course id is valid but row not found | src/screens/study/CourseDetailScreen.tsx:48,127-140
    PROBLEM: If route.params.courseId is missing the screen returns null (blank screen, no back affordance) at line 48. Separately, if the course query returns no row (deleted course, RLS denies the row), `course` stays null and after loading finishes the screen shows only a SubBar plus a bare '—' with no explanation or retry. The materials/questions/books still loaded but are never shown because the early `if (!course)` block returns before the list. A non-member who opens a stale CourseDetail link gets a dead end.
    FIX: For the missing-param case render an error state with a back button instead of null. For the not-found case show a proper empty/error message (e.g. 'Course not found or you don't have access') rather than '—'.
- [low] toggleVisibility ignores Supabase error and optimistically updates anyway | src/screens/study/StudyHubScreen.tsx:744-750
    PROBLEM: toggleVisibility() awaits the update but never checks `error`. It unconditionally sets local state to the new value and flashes 'Visibility updated'. If RLS or a constraint rejects the update (e.g. a non-CR somehow reaching this, or a network failure) the UI shows the section as flipped while the DB is unchanged, desyncing until the next load().
    FIX: Capture `{ error }` from the update; on error toast the failure and do NOT mutate local state (or revert it). Mirror the error handling used by the other mutations in this file.

### FINDINGS lane (10)
- [high] ReportDetail non-owner fallback reads columns the campus_reports RPC never returns (assigned_staff_id, photo_url) | src/screens/reports/ReportDetailScreen.tsx:104
    PROBLEM: When a non-owner/non-assignee/non-admin opens a report, the RLS-protected direct select returns null (line 85 maybeSingle), so the screen falls back to the campus_reports RPC (line 93-94) and sets that row as the full `report`. But the RPC's RETURNS TABLE only exposes id, code, category, description, building, room, status, reporter_id, reporter_name, created_at. It does NOT return assigned_staff_id, photo_url, updated_at, or deleted_at. Consequences for a student viewing someone else's report: (1) report.assigned_staff_id is undefined so the assignee lookup at line 104 never runs and the 'Assigned to' block never renders even when staff are assigned; (2) report.photo_url is undefined so the attached photo (line 233) never shows; (3) isAssignedStaff (line 72) reads undefined assigned_staff_id. This is a real data-visibility defect, not cosmetic — the detail view silently drops fields for the campus-feed path.
    FIX: Extend the campus_reports SQL RPC to also return assigned_staff_id and photo_url, and update CampusReport type, OR on the fallback path fetch the missing fields separately. At minimum guard the photo/assignee rendering so it degrades cleanly, and document that the fallback report is partial.
- [medium] campus_reports RPC has no per-row privacy filter — every report description/photo-less metadata is exposed to all authenticated users | src/services/reportsService.ts:36
    PROBLEM: getCampusReports calls the SECURITY DEFINER campus_reports RPC which selects every non-deleted report (where r.deleted_at is null) with reporter full_name, bypassing the owner/assigned/admin reports_select RLS policy. MyReportsScreen renders this for all students by default (scope 'everyone'). Any student can read the full description text and reporter real name of every maintenance report campus-wide. If reports can contain sensitive content (safety/security incidents, personal info), this is an intentional-but-risky data exposure. Confirmed: the RPC has no filter beyond deleted_at and is SECURITY DEFINER.
    FIX: Confirm this campus-wide exposure is intended for the 'Safety / Security' category and reporter names. If not, restrict campus_reports (e.g. exclude sensitive categories, or return reporter_name only for non-sensitive rows) or gate the 'everyone' scope behind a role.
- [medium] Detail screen and all reports lists have no refresh-on-focus; stale data after status change / edit / delete | src/screens/reports/MyReportsScreen.tsx:67
    PROBLEM: MyReportsScreen, AssignedToMeScreen, and ReportDetailScreen load only via useEffect on mount (MyReports line 67, Assigned line 76, Detail line 110). None use useFocusEffect even though 7 other screens in the app do (HomeScreen, NotificationsScreen, dashboards, etc.). Flow that breaks: user opens ReportDetail, changes status (admin) or declines, navigates back to MyReports/AssignedToMe — the list still shows the old status because the screen is already mounted and never refetches on focus. Similarly editing a report via ReportForm then going back to the list shows stale title/location. The user must manually pull-to-refresh.
    FIX: Wrap load() in useFocusEffect(useCallback(() => { load(); }, [load])) in MyReportsScreen, AssignedToMeScreen, and ReportDetailScreen so the data refetches when the screen regains focus, matching the pattern already used elsewhere.
- [medium] ReportForm edit path uses raw supabase.update and swallows RLS no-op as success | src/screens/reports/ReportFormScreen.tsx:168
    PROBLEM: On edit, handleSubmit calls supabase.from('reports').update(payload).eq('id', editReportId). Supabase update returns error=null even when RLS matches zero rows (no row updated). If the report is no longer Open (RLS reports_update likely restricts edits to status='Open' owners) or ownership changed, the update silently affects 0 rows but error is null, so the code calls navigation.goBack() (line 173) as if it succeeded. The user believes their edit saved when nothing changed.
    FIX: Add .select() to the update and verify data.length > 0 (or check returned row) before treating it as success; show an error if zero rows were updated.
- [medium] Edit prefill destroys structured location, silently corrupting building/floor/place on save | src/screens/reports/ReportFormScreen.tsx:96
    PROBLEM: When editing, the prefill (lines 94-99) cannot reverse the structured location string built at submit (e.g. 'Ground floor · Classroom 204'). It regex-extracts only a building digit, then hardcodes setFloor('Ground') and setPlace('Other'), dumping the entire original room string into locNote. On re-save, handleSubmit (lines 154-158) rebuilds room as 'Ground floor · Other · <original room string>', so a report originally '2nd floor · Lab 5' silently becomes 'Ground floor · Other · 2nd floor · Lab 5' even if the user only changed the title. Each edit further corrupts the location.
    FIX: Either parse the stored building/room back into the structured fields properly, or store structured location in dedicated columns instead of a concatenated string so edits round-trip losslessly.
- [low] Admin can set any status including illegal transitions; client offers all 5 with no transition guard | src/screens/reports/ReportDetailScreen.tsx:79
    PROBLEM: statusChoices for admin = all five STATUS_OPTIONS (line 79), and handleStatusUpdate (line 121) does a raw update with no transition validation, e.g. admin can move a 'Closed' or 'Resolved' report back to 'Open'. handleStatusUpdate also does not write a report_events row, so the timeline (built from report_events, lines 181-185) will not reflect admin status changes — only whatever DB trigger exists records them. If no trigger logs admin updates, the timeline silently omits them.
    FIX: Verify a DB trigger logs report_events on every status update; if not, insert an event. Optionally constrain admin transitions to a sane state machine.
- [low] handleStatusUpdate optimistic update without refetch leaves timeline desynced | src/screens/reports/ReportDetailScreen.tsx:127
    PROBLEM: After a successful status update, the code only does setReport(prev => ({...prev, status})) (line 127). It does not re-run load(), so the events/timeline (events state) is not refreshed — a new report_events row created by a DB trigger won't appear until manual pull-to-refresh. The header pill updates but the timeline below it stays stale, producing an inconsistent screen.
    FIX: Call load() after a successful status update (or await it) so events and report are both refreshed.
- [low] Dead, miswired useReports hook: hardcoded page size and never consumed | src/hooks/useReports.ts:25
    PROBLEM: useReports is exported but imported nowhere in the codebase (grep shows only its own definition). It also hardcodes hasMore = result.data.length === 20 (line 25) instead of the PAGE_SIZE constant used by the service (constants/app.ts PAGE_SIZE=20); if PAGE_SIZE ever changes, pagination breaks. Additionally setLoading(false) at line 30 runs on every load including non-reset loadMore calls even though loading was never set true for those, which is harmless but indicates the loading/refreshing bookkeeping was never exercised. This is unused, untested code that will silently rot.
    FIX: Either delete useReports.ts (and getReports, which is also only used by this dead hook), or wire it up and replace the literal 20 with PAGE_SIZE.
- [low] MyReports/AssignedToMe lists render via .map inside a ScrollView (no virtualization, no keyExtractor safety) | src/screens/reports/MyReportsScreen.tsx:214
    PROBLEM: Both lists render all rows with filtered.map inside a plain ScrollView (MyReports line 213-214, Assigned line 163-164). The campus feed fetches up to 200 rows (getCampusReports default limit 200) and all are mounted at once with no FlatList virtualization. On large campuses this mounts hundreds of card components plus images eagerly, causing memory/scroll-jank. There is also no pagination on these screens despite the service supporting it.
    FIX: Convert to FlatList with keyExtractor={r => r.id} and windowing; consider paginating the campus feed.
- [low] Detail screen reporter/assignee profile fetch has no error handling and races with status reads | src/screens/reports/ReportDetailScreen.tsx:100
    PROBLEM: load() fires sequential profile lookups (lines 100-107) after setting state, with results ignored on error (only `if (data)`programmatic checks). If load() is re-invoked (focus/refresh) while a prior in-flight request resolves, there is no cancellation/guard, so a stale assigneeName/reporterName from a previous reportId could overwrite the current one. reportId can also change if the param report differs from fetched. Low likelihood but unguarded.
    FIX: Add an isMounted/abort guard (or ignore-stale-response flag keyed on reportId) around the async setState calls in load().

### FINDINGS lane (12)
- [high] Next-prayer logic never matches Jummah due to key spelling mismatch ('jumuah' vs DB 'jummah') | src/screens/prayer/PrayerScreen.tsx:40
    PROBLEM: computeNext()'s isToday() guard checks p.key !== 'jumuah', and the month table filters with p.key !== 'jumuah' (lines 225, 247). But the DB key (verified) and the TS PrayerTime interface (database.ts:208) both spell it 'jummah'. Because the literal 'jumuah' never equals the real key 'jummah', the special Friday-only handling is dead code: the Jummah row is always treated as a normal prayer, so on non-Fridays Jummah can be picked as the 'next prayer' and it is never filtered out of the month grid (it appears as an extra azan column).
    FIX: Replace every 'jumuah' literal with 'jummah' (lines 40, 225, 247) so it matches the DB/TS key. Better, reference a shared constant to avoid future drift.
- [high] Inserting NULL into musallah_locations.floor_desc violates NOT NULL constraint | src/screens/prayer/PrayerScreen.tsx:110
    PROBLEM: saveMusallah() writes floor_desc: musEdit.floor_desc.trim() || null on both update (line 110) and insert (line 116). The live column musallah_locations.floor_desc is NOT NULL. When an admin leaves the floor/description blank, Supabase rejects the write with a not-null violation and the toast shows a raw Postgres error; the location is never saved.
    FIX: Default empty to '' instead of null: floor_desc: musEdit.floor_desc.trim(). Or make the column nullable in a migration if blank descriptions should be stored as NULL.
- [medium] musallah_locations id is smallint but typed/handled as string | src/screens/prayer/PrayerScreen.tsx:32
    PROBLEM: The Musallah interface declares id: string and musEdit is { id: string | null }. The live column musallah_locations.id is smallint. The .eq('id', m.id) / .delete().eq('id', m.id) calls still work because PostgREST coerces the value, but the typing is wrong and any string comparison on id (e.g. Set/key logic elsewhere) is misleading. Also the Musallah.sort is typed sort: number | null while the column is NOT NULL.
    FIX: Type id as number and sort as number to match the schema; keys in the JSX (key={m.id}) already coerce fine, but the type lie can hide real comparison bugs.
- [medium] Prayer screen does not refresh on focus; admin edits / stale data not reflected | src/screens/prayer/PrayerScreen.tsx:85
    PROBLEM: Data is loaded only via useEffect(() => { load(); }, [load]) on mount. There is no useFocusEffect/navigation focus listener. When an admin edits a jamaat time or musallah on this screen the local load() runs, but if jamaat/musallah are changed elsewhere, or the user navigates back to a previously-mounted Prayer screen, the times remain stale until manual pull-to-refresh. The project standard (per memory: refresh-on-focus added in c663282) is to refresh on focus.
    FIX: Use useFocusEffect(useCallback(() => { load(); }, [load])) instead of/in addition to the mount effect.
- [medium] load() swallows query errors — failures show as empty/stale with no feedback | src/screens/prayer/PrayerScreen.tsx:76
    PROBLEM: load() destructures only { data } from both Promise.all results and never inspects error. If prayer_times or musallah_locations fails (RLS, network), prayers stays [] and the screen shows the 'No prayer times available' empty state as if there were genuinely no data, with no error toast. Same anti-pattern in BusScreen.load (BusScreen.tsx:47-54) and BusDetailScreen effect (BusDetailScreen.tsx:23-27) which ignores error and leaves a permanent spinner on failure.
    FIX: Capture error from each query and surface a toast / error state; in BusDetail, set an error flag so the ActivityIndicator does not spin forever when the fetch fails.
- [high] BusDetailScreen calls useState after a conditional early-return — Rules of Hooks violation | src/screens/bus/BusDetailScreen.tsx:19
    PROBLEM: The component does `if (!id) return null;` on line 19 BEFORE `const [busRoute, setBusRoute] = useState(...)` on line 20 and the useEffect on line 22. Hooks are called conditionally: if a route is ever rendered with id present then later without (or vice-versa), React throws 'Rendered fewer hooks than expected' and crashes. Even if id is currently always set, this is a latent crash and breaks the Rules of Hooks.
    FIX: Move all hook calls to the top of the component, then guard rendering after hooks: declare state/effect first, and render the null/guard branch from inside the returned JSX or after the hooks run.
- [medium] saved_bus_routes upsert relies on implicit conflict target; optimistic toggle has no error rollback | src/screens/bus/BusScreen.tsx:64
    PROBLEM: toggleSave optimistically mutates savedIds, then awaits delete/upsert but never checks the returned error. If the network/RLS write fails, the star stays toggled (UI desync) with no toast. The upsert (line 71) has no onConflict specified; it works only because the PK is (user_id, route_id) (verified), but it is fragile — any added surrogate/default column would change conflict resolution.
    FIX: Check the { error } from delete/upsert and revert the optimistic Set on failure (and toast). Pass onConflict: 'user_id,route_id' explicitly to the upsert.
- [low] toggleSave can fire before user is loaded / multiple rapid taps cause desync | src/screens/bus/BusScreen.tsx:64
    PROBLEM: toggleSave guards `if (!user) return;` but load() queries saved_bus_routes with .eq('user_id', user?.id ?? '') (line 50) — when user is briefly null it queries user_id='' returning nothing, and savedIds is set to empty; a subsequent real load is only triggered by the [user?.id] dependency. Rapid double-taps on the star are not debounced, and because each tap toggles based on current savedIds without awaiting, fast taps can leave local state and DB out of sync.
    FIX: Skip the saved query entirely while user?.id is undefined (return early in load) rather than querying with ''. Debounce/disable the star while a save request is in flight.
- [low] nextDeparture string comparison breaks across midnight and on unsorted times | src/screens/bus/BusScreen.tsx:117
    PROBLEM: nextDeparture finds the first to_departures entry where d > hhmm using lexicographic string compare. This assumes to_departures is sorted ascending (admin enters them free-form, comma-separated, no sort applied in saveRoute) and that single-digit hours are zero-padded. An entry like '9:00' (not '09:00') sorts after '13:00', and unsorted input yields a wrong 'Next'. Same logic duplicated in BusDetailScreen.tsx:38.
    FIX: Normalize/zero-pad and sort to_departures (and validate HH:MM) on save, or parse to minutes and compare numerically when computing the next departure.
- [low] New bus route id derived from name can collide / produce empty id | src/screens/bus/BusScreen.tsx:109
    PROBLEM: On insert the id is generated by slugifying form.name (lowercase, non-alphanumeric to '-', sliced to 40). A name with only non-ASCII (e.g. Bangla) characters produces an empty or all-dash id, and two routes with the same/similar name collide on the text PK, causing an insert error (or silent overwrite expectations). No uniqueness check or fallback.
    FIX: Append a short random/uuid suffix or use a DB-generated id; validate the slug is non-empty before insert.
- [low] BusDetail renders departures/stops without null guards on array columns | src/screens/bus/BusDetailScreen.tsx:112
    PROBLEM: busRoute.to_departures.map (line 112) and busRoute.from_departures.length (line 120) and busRoute.stops (line 46) are accessed directly without ?? []. The columns are NOT NULL arrays in the live DB so this is currently safe, but nextDeparture uses (busRoute?.to_departures ?? []) defensively while the render path does not — inconsistent, and a single bad row (or a future schema change) crashes the detail screen.
    FIX: Use (busRoute.to_departures ?? []) consistently in the render, matching nextDeparture's guarding.
- [low] Prayer/jamaat edit and route save lack input validation beyond regex / no trimming of time arrays | src/screens/prayer/PrayerScreen.tsx:95
    PROBLEM: saveJamaat validates jamaatInput against /^\d{1,2}:\d{2}$/ which accepts impossible times like '99:99' and single-digit hours that then break the lexicographic nextDeparture/computeNext comparisons (computeNext parses with split(':') so '99:99' yields h=99). Bus to_departures/from_departures are stored verbatim with no HH:MM validation.
    FIX: Validate hours 0-23 and minutes 0-59 and zero-pad before storing; apply the same validation to bus departure time arrays.

### FINDINGS lane (8)
- [high] Requester identities fetched for every viewer (UI-only gating, data leak) | src/screens/rides/RideDetailScreen.tsx:50-64
    PROBLEM: The third query in the mount effect (`takenRes`) selects `requester_id, profiles:requester_id(full_name)` for ALL requests on the ride, unconditionally, for any user who opens the detail screen. The `ride_requests` SELECT RLS policy is `qual: true` (verified: anyone can read every row), and the joined profile full_name comes back in the network payload regardless of role. The code only *hides* the list in the UI behind `if (rideRes.data.driver_id === user?.id)`, but the requesters' names are already on the wire for any non-driver who inspects the response. This exposes who requested seats on someone else's ride.
    FIX: Only fetch the requester list + names when the current user is the driver. Either split into two queries (a lightweight count for everyone, and a separate name-bearing query gated on `ride.driver_id === user.id`), or move requester enumeration behind a SECURITY DEFINER RPC that checks `auth.uid() = driver_id`. Tighten the `ride_req_select` RLS so a non-driver/non-requester cannot read others' rows.
- [medium] No refresh-on-focus: lists/detail show stale data after mutations | src/screens/rides/RidesScreen.tsx:78
    PROBLEM: RidesScreen loads data only via `useEffect(() => { load(); }, [load])` on mount. After posting a ride in RidePostScreen (which calls `navigation.goBack()`), returning to the list does NOT re-fetch, so the newly posted ride is missing until manual pull-to-refresh. Same pattern in RideDetailScreen:36-77 (mount-only effect) — taken counts / requester list go stale while the screen stays mounted in the stack. The codebase already uses `useFocusEffect` in HomeScreen, NotificationsScreen, StaffDashboardScreen, AdminDashboardScreen, so this is an inconsistent omission.
    FIX: Wrap `load` in `useFocusEffect(useCallback(() => { load(); }, [load]))` (from @react-navigation/native) in RidesScreen, and re-run the detail fetch on focus in RideDetailScreen, so data refreshes when navigating back.
- [medium] Missing error/empty handling when rides query fails | src/screens/rides/RidesScreen.tsx:57-75
    PROBLEM: `load` only acts when `ridesRes.data` is truthy and silently ignores `ridesRes.error`. On a network/RLS failure, `rides` stays at its previous value (or empty), `takenCounts`/`requestedIds` are never updated, and there is no error state or toast — the user sees a misleading empty state or stale list with no indication of failure. There is also no initial loading indicator (the screen renders the empty state during the first fetch before data arrives).
    FIX: Capture and surface `ridesRes.error` (toast or inline error banner), and add a loading flag so the first fetch shows a spinner instead of the 'no rides' empty state.
- [medium] seatsLeft optimistic desync vs. server taken count in list | src/screens/rides/RidesScreen.tsx:86-91
    PROBLEM: `requestRide` on the list updates only `requestedIds` (adds the id) but never increments `takenCounts[rideId]`. So after requesting, the card still shows the pre-request seat count (`seatsLeft = seats_total - taken`), and the seats-left figure is wrong until a full reload. The detail screen handles this correctly (increments takenCount on non-dup), but the list does not, producing inconsistent seat numbers between the two screens.
    FIX: In `requestRide`, on a brand-new insert (no error) increment `setTakenCounts(prev => ({ ...prev, [rideId]: (prev[rideId] ?? 0) + 1 }))`; skip the increment on a 23505 duplicate (already counted).
- [low] UTC date boundary can hide same-day rides for Bangladesh users | src/screens/rides/RidesScreen.tsx:51
    PROBLEM: The filter `.gte('date', new Date().toISOString().split('T')[0])` derives 'today' from the UTC date. The app targets Bangladesh (UTC+6, fares in ৳, days Sat-first). Between 00:00 and 06:00 local time, `toISOString()` still returns the previous UTC calendar day, so a ride posted for the current local day could be excluded (or yesterday's still shown) depending on the boundary. The comparison is also date-only, so a ride whose time has already passed earlier today is still listed.
    FIX: Compute the local date (e.g. via a UTC+6 offset or a locale-aware helper) for the `gte` bound, and consider filtering on date+time so already-departed same-day rides drop off.
- [low] Hardcoded English UI strings bypass i18n dictionary | src/screens/rides/RidePostScreen.tsx:258
    PROBLEM: The submit button label is the literal string 'Offer Ride' instead of a `t.rides2.*` key, while the screen title (line 132) correctly uses `t.rides2.offerRideTitle`. Similarly RidesScreen:203 ('Request sent'), RideDetailScreen:144/157 (title 'Ride Detail'), :248 ('SEAT REQUESTS (...)'), and the Alert copy in RideDetailScreen:93 ('Delete this ride?'/'Your seat requests will be discarded.') and :128 are hardcoded English, breaking EN/বাং parity that the rest of these screens follow.
    FIX: Route all these literals through the i18n dictionary (add `t.rides2.*` keys) like the surrounding labels.
- [low] Driver requester names default to literal 'Student' instead of i18n fallback | src/screens/rides/RideDetailScreen.tsx:62
    PROBLEM: When a requester profile has no full_name, the code falls back to the hardcoded string `'Student'`, whereas the rest of the screen uses i18n fallbacks (e.g. `t.rides2.unknown`, `t.rides2.driverFallback`). Inconsistent and not translated.
    FIX: Use an existing i18n fallback key (e.g. `t.rides2.unknown`) instead of the literal 'Student'.
- [low] Date validation accepts impossible calendar dates | src/screens/rides/RidePostScreen.tsx:102
    PROBLEM: Date validity is checked only with the regex `/^\d{4}-\d{2}-\d{2}$/`, which accepts impossible values like 2026-13-40 or 2026-02-31. There is also no check that the date is not in the past, so a user can post a ride for a date that will never appear in the list (which filters `date >= today`). Time has no format validation at all (any string is stored; only empty defaults to 08:00).
    FIX: After the regex, parse with `new Date(date)` and verify the components round-trip and the date is today or later; optionally validate `time` against an HH:MM pattern.

### FINDINGS lane (10)
- [high] club_member_counts RPC is referenced but does not exist in any migration | src/screens/clubs/ClubsScreen.tsx:51
    PROBLEM: ClubsScreen calls supabase.rpc('club_member_counts') to get accurate per-club member counts that bypass per-row RLS. No migration defines this function (grep over supabase/ finds zero definitions; only 0053 and 0054 exist and neither creates it). At runtime countsRes.error is set, countsRes.data is null, so countMap stays empty and every card falls back to `c.club_members?.length ?? 0`. Because club_members_select RLS (0053:190) only returns member rows for clubs the current user has joined, the embedded `club_members!left` join returns rows ONLY for the viewer's own clubs. Net effect: every club the user has NOT joined shows '0 members', and the user_role detection still works only for joined clubs. The whole reason the RPC exists (the inline comment at line 49-50) is defeated. ClubDetailScreen has the same blind spot: members.length (line 193) is correct only for clubs you belong to since the same RLS gate applies.
    FIX: Add the missing migration defining public.club_member_counts() returning (club_id uuid, members bigint) as a SECURITY DEFINER function (grant to authenticated, revoke from anon), e.g. select club_id, count(*) from club_members group by club_id. Until then, surface countsRes.error instead of swallowing it. Confirm the live DB actually has this function before shipping.
- [medium] ClubsScreen swallows query errors and shows a false 'no clubs' empty state | src/screens/clubs/ClubsScreen.tsx:53
    PROBLEM: load() does `if (clubsRes.error) return;` with no error state set and no toast. On any failure (network, RLS, schema) the screen keeps clubs=[] and renders the 'noClubsFound' empty state, which is indistinguishable from a genuinely empty list. There is also no loading indicator: on first mount clubs is [] so the user briefly sees 'No clubs found' before data arrives. Every other clubs screen uses useToast for errors; this one has none imported.
    FIX: Track loading and error states; show a spinner while the first load is in flight, and a distinct error view (or toast) when clubsRes.error is set, separate from the empty state.
- [medium] ClubManageScreen seeds category with 'other', an invalid value that breaks the picker and save | src/screens/clubs/ClubManageScreen.tsx:55
    PROBLEM: On load, `setCategory(clubRes.data.category ?? 'other')`. The CATEGORIES list (line 24) is ['Tech','Cultural','Sports','Professional','Social'] and contains no 'other'. If a club's category is ever null, the category state becomes 'other', which (a) highlights no chip so the UI shows no selection, and (b) if the user saves without picking a real chip, club_update_details rejects it: the RPC raises 'Invalid category.' (0054:56-58, only the five values allowed). The save then fails with a raw exception message. The default fallback should be a valid category.
    FIX: Default to a valid category: `setCategory(clubRes.data.category && CATEGORIES.includes(clubRes.data.category) ? clubRes.data.category : 'Tech')`.
- [medium] ClubPostScreen hardcodes English UI strings despite existing i18n keys | src/screens/clubs/ClubPostScreen.tsx:69
    PROBLEM: The title placeholder 'Title (optional)' (line 69), body placeholder 'Share an update with members…' (line 77), submit label 'Post' (line 92), and the error message 'Could not post. Please try again.' (line 45) are all hardcoded English. The dictionary already defines t.clubs2.titlePlaceholder, t.clubs2.shareUpdatePlaceholder, and t.clubs2.postBtn (en.ts:372-374 / bn.ts:373-375). Bengali users see English in this screen, and the catch-block error is not localized at all.
    FIX: Use t.clubs2.titlePlaceholder, t.clubs2.shareUpdatePlaceholder, t.clubs2.postBtn, and a localized error key in place of the literals.
- [low] ClubPostScreen does not refresh the detail feed after posting (stale state) | src/screens/clubs/ClubPostScreen.tsx:43
    PROBLEM: After a successful insert it only calls navigation.goBack(). It works in practice only because ClubDetailScreen registered a navigation 'focus' listener (ClubDetailScreen.tsx:79) that re-runs load on focus. There is no explicit signal; if that focus listener is ever removed or the navigator reuses the screen without firing focus, the new post will not appear. The coupling is implicit and undocumented.
    FIX: Acceptable as-is given the focus listener, but make it explicit — pass a route param / use navigation events, or have ClubPost trigger an onPosted callback so the dependency is not silently load-bearing.
- [low] ClubMembersScreen search effect re-runs on every members mutation, resetting/refetching results | src/screens/clubs/ClubMembersScreen.tsx:72
    PROBLEM: The debounced student-search effect lists `members` in its dependency array (line 72). Every time a member is added/removed/role-changed, load() updates the members array, which re-triggers this effect: it tears down and re-creates the 250ms timer and re-issues the profiles query even though the query text did not change. members is only used inside to build the exclusion Set, which does not need to drive a refetch. This causes redundant network calls and can momentarily clear results while the add sheet is open right after adding someone.
    FIX: Drop `members` from the dependency array and read the latest members via a ref, or compute the exclusion filter at render time over the existing results instead of refetching when members changes.
- [low] ClubMembersScreen has no null guard on clubId and no empty/loading state | src/screens/clubs/ClubMembersScreen.tsx:35
    PROBLEM: clubId is read as `route.params?.clubId` and typed as `string` but never validated. If the screen is ever reached without the param (deep link, bad navigation), clubId is undefined and load() issues `.eq('club_id', undefined)` which returns no rows; the card renders empty with no message. Unlike the other screens there is no ActivityIndicator on first load and no early return / empty state, so the user sees a bare empty card. Sibling screens (ClubManageScreen.tsx:32, ClubPostScreen.tsx:28) guard `if (!clubId) return null;`.
    FIX: Add `if (!clubId) return null;` (or an error view) like the sibling screens, and render a loading/empty state for the members list.
- [low] ClubDetail togglePin reloads full screen instead of optimistic/local update, causing flicker and races | src/screens/clubs/ClubDetailScreen.tsx:138
    PROBLEM: togglePin() updates the row then calls load(), which re-fetches club + posts + members (3 queries) and re-sorts. Rapid double-taps fire two updates and two full reloads; the second toggle can land before the first reload returns, so the displayed pin state can briefly disagree with the DB. deletePost (line 149) and leaveClub (line 129) do optimistic local state updates, so the pattern is inconsistent. There is also no disabling of the button during the in-flight update.
    FIX: Optimistically flip p.is_pinned in local posts state (re-sorting locally) and revert on error, matching deletePost/leaveClub; or at minimum guard against concurrent taps.
- [low] ManageClubsScreen creates a club with no president and computes counts client-side under RLS | src/screens/dashboard/ManageClubsScreen.tsx:98
    PROBLEM: createClub() inserts a club but never creates any club_members row, so a freshly created club has zero members and no president until an admin separately runs 'Assign President'. The card meta will read '0 members'. Separately, load() computes member_count and president_name by client-side filtering of a full club_members select (lines 60,64-67); this is correct for admins (club_members_select grants is_admin full visibility) and this screen is admin-gated (line 130), so it works — but it is O(clubs × members) on the client and unbounded (no limit on the members query), which will not scale.
    FIX: Either auto-create a president membership on club creation (or prompt to assign one immediately), and replace the unbounded client-side member tally with the same server-side count RPC the rest of the feature should use.
- [low] ClubManageScreen 'Transfer presidency' is admin-only by design but the screen is reachable by president/VP, who see a save-only screen with a dead section | src/screens/clubs/ClubManageScreen.tsx:177
    PROBLEM: ClubDetailScreen opens ClubManage for canManage = president/vp/admin (ClubDetailScreen.tsx:115,167). The transfer-presidency section is gated to profile.role === 'admin' (line 177) because club_set_president raises unless is_admin() (0054:82). That is correct, but it means a president/VP entering 'Manage Club' gets only the edit form with no way to hand off presidency, while the inline comment frames transfer as a club-officer action. Minor UX/role-model mismatch, not a crash. Also note saveChanges (line 64-66) navigates back on success but does not refresh the parent detail screen except via its focus listener.
    FIX: Either document that presidency transfer is admin-only and remove the misleading framing, or expose a president-initiated handover RPC. Confirm intended role model.

### FINDINGS lane (10)
- [high] Removed job is unreadable by non-admins, leaving JobDetail stuck on an infinite spinner | src/screens/jobs/JobDetailScreen.tsx:47-56
    PROBLEM: loadJob() does supabase.from('jobs').select('*').eq('id', jobId).single() with no deleted_at handling. The jobs_select RLS is `deleted_at IS NULL OR is_admin()`, so when a non-admin opens a job that has since been removed (stale browse list, saved/bookmarked item, or deep link), the query returns zero rows; .single() yields an error and no data. setJob is never called, so `job` stays null and the screen renders the ActivityIndicator branch (line 109-116) forever with no error/empty state. The removed-listing banner UI at lines 199-205 is therefore dead code for every non-admin (they can never load a row where deleted_at is set).
    FIX: Use .maybeSingle() and handle the null/error case with an explicit 'listing unavailable' empty state instead of an indefinite spinner. Detect jobRes.error and render a not-found view; only admins will ever receive a deleted_at row, which is the only case the removed banner can display.
- [medium] Job interface declares a non-existent `status` column and dead 'closed' status branch | src/screens/jobs/JobsBrowseScreen.tsx:49
    PROBLEM: The local Job interface declares `status: string`, but the jobs table has no `status` column (verified: columns are id, code, title, company, job_type, location, work_mode, description, requirements, stipend, deadline, apply_method, apply_value, apply_file_url, apply_file_name, posted_by, posted_by_name, club_id, deleted_at, removed_by, removed_reason, created_at, updated_at). Status is computed client-side via computeJobStatus() from deadline/deleted_at, so the declared field is always undefined. Relatedly, jobStatusTone has a 'closed' case (line 33) and JobDetailScreen jobStatusTone has 'closed' (line 23) that computeJobStatus / computedStatus can never produce ('open'|'closing'|'expired'|'removed' only), so it is dead code. Misleading and a latent bug if any code starts trusting job.status.
    FIX: Remove `status` from the interface (it is never selected meaningfully) and drop the unreachable 'closed' branch, or derive status consistently in one place.
- [medium] Silent failure: jobs/bookmarks load swallows all errors with no error state | src/screens/jobs/JobsBrowseScreen.tsx:78-87
    PROBLEM: load() destructures jobsRes/savedRes and only acts on .data ('if (jobsRes.data) setJobs(...)'). Errors (network failure, RLS rejection) are never checked or surfaced; on failure the list silently shows the empty 'Nothing found' state, indistinguishable from a genuinely empty result. Same pattern in JobsModerateScreen.load() (lines 41-54) and JobDetailScreen.loadJob() (lines 47-54).
    FIX: Track an error/loading flag; on jobsRes.error show a retry/error UI distinct from empty. At minimum log/toast the error.
- [medium] No refresh-on-focus: stale list and stale detail after mutations | src/screens/jobs/JobsBrowseScreen.tsx:87
    PROBLEM: Loading is done via useEffect(() => { load(); }, [load]) which runs once on mount. After posting a job (JobPostScreen navigates back), withdrawing/removing/restoring in JobDetailScreen (navigation.goBack), or returning from JobsModerate, JobsBrowseScreen does not reload — the new/removed job is missing or stale until manual pull-to-refresh. The project memory notes refresh-on-focus was specifically added elsewhere (commit c663282) for exactly this class of bug. JobsModerateScreen has the same gap (line 56).
    FIX: Use useFocusEffect(useCallback(() => { load(); }, [load])) from @react-navigation/native so the list/moderation screens reload when refocused after a mutation.
- [medium] Bookmark toggle is optimistic with no rollback on failure (state desync) | src/screens/jobs/JobsBrowseScreen.tsx:109-121
    PROBLEM: toggleSave updates local savedIds (setSavedIds(next)) and fires the insert/delete without awaiting/checking the result for errors before committing UI state — the await is not error-checked. If the insert/delete fails (duplicate, RLS, offline), the UI shows the toggled state but the DB disagrees; on next load the star flips back unexpectedly. JobDetailScreen.toggleSave (lines 58-66) has the same fire-and-forget pattern with setSaved(!saved). The two screens also do not share bookmark state, so saving in detail does not reflect in the browse list until reload.
    FIX: Check the mutation error and revert the local set on failure (toast the error). Consider centralizing bookmark state so detail and browse stay in sync.
- [low] Report submission shows no success feedback and never refreshes detail | src/screens/jobs/JobDetailScreen.tsx:68-78
    PROBLEM: submitReport closes the sheet and clears reason on success but gives no confirmation toast (unlike error path). User cannot tell whether the report was filed and may re-submit. Also it does not reload, so any report-count-derived UI stays stale.
    FIX: On success show a confirmation toast (e.g. 'Report submitted') and clear/close as now.
- [medium] Deadline accepts free-text input that can violate the NOT NULL date column and RLS deadline check | src/screens/jobs/JobPostScreen.tsx:71,102
    PROBLEM: deadline is a plain TextInput (line 175-181) inserted as deadline.trim() || dhakaToday into a column that is `date NOT NULL`. Any non-ISO text a user types (e.g. '30 June', '06/30/2026') will fail the Postgres date cast and the insert errors out; canSubmit (lines 80-84) does not validate deadline format at all, so the only feedback is a raw Postgres error message in the toast. A past date also fails the RLS deadline >= today check noted in the code comment.
    FIX: Use a date picker or validate YYYY-MM-DD and not-in-past before enabling submit; surface a friendly validation message instead of relying on the DB cast error.
- [low] apply_method 'file' branch unreachable from the post UI but handled in detail | src/screens/jobs/JobPostScreen.tsx:74
    PROBLEM: JobPostScreen only allows applyMethod 'email' | 'link' (state type and SegControl at line 206-211), so apply_file_url / apply_file_name (real columns) are never set from this app. JobDetailScreen (lines 227-228) handles apply_method === 'file' via apply_file_url. The file branch is effectively dead for app-created jobs; if a job with method 'file' but null apply_file_url exists, Apply Now silently falls through to the generic info toast.
    FIX: Either add file upload support in JobPost or document that 'file' is web-only; ensure the detail Apply handler degrades gracefully (it does via the else toast).
- [low] Linking.openURL not awaited / not guarded for unsupported URLs | src/screens/jobs/JobDetailScreen.tsx:222-232
    PROBLEM: The Apply Now handler calls Linking.openURL(job.apply_value) / mailto: / apply_file_url without await or try/catch and without canOpenURL. A malformed apply_value (user-entered, no URL validation on post) throws an unhandled promise rejection and nothing opens, with no feedback to the user.
    FIX: await Linking.openURL inside try/catch (optionally Linking.canOpenURL first) and toast a 'Could not open link' message on failure.
- [low] Moderation loads all reports unfiltered and joins client-side (scales poorly, partial data risk) | src/screens/jobs/JobsModerateScreen.tsx:43-53
    PROBLEM: load() fetches ALL jobs (no limit) and ALL job_reports (no limit, no pagination) then joins in JS by job_id. With growth this transfers the entire reports table on every refresh and PostgREST will silently cap rows at the default limit (1000), so beyond that the reasons/counts shown to admins become incorrect with no indication.
    FIX: Aggregate server-side (RPC or a view returning reported jobs with counts) or at least paginate and bound the queries; surface when results are truncated.

### FINDINGS lane (11)
- [medium] ISSUE_MAP icon lookup never matches multi-word categories (IT / Network, Safety / Security) | src/screens/dashboard/AdminDashboardScreen.tsx:86
    PROBLEM: ISSUE_MAP keys are snake_case single tokens: it_network, safety, cleanliness, etc. The lookup is ISSUE_MAP[r.category?.toLowerCase()]. DB category values (verified live: 'Cleanliness','Plumbing','Electrical', and the trade options include 'IT / Network' and 'Safety / Security') lowercase to 'it / network' and 'safety / security', which are not keys in ISSUE_MAP. So those two categories always fall through to the generic 'other' icon (sliders) instead of wifi / shield. Same bug in StaffDashboardScreen.tsx:78 with the identical ISSUE_MAP.
    FIX: Key ISSUE_MAP by the exact category strings used in the app ('IT / Network','Safety / Security', etc.) and drop the toLowerCase(), or normalize both sides through a shared slug map. Ensure keys match the canonical TRADES/category list in ManageUsersScreen.tsx:24.
- [high] Report assignment writes have no error handling (silent failure + desynced UI) | src/screens/dashboard/AdminDashboardScreen.tsx:184
    PROBLEM: assignReport calls supabase.from('reports').update(...).eq('id', reportId) and ignores the returned { error }. If RLS or a network error rejects the update, the modal still closes and load() is called; the user sees no error and (since load() refetches) the report silently stays unassigned. AllReportsScreen.tsx:80-89 is worse: it does the same un-checked update AND optimistically mutates local state to 'In Progress', so on a failed write the list shows the report as assigned/In Progress while the DB row is unchanged — a true optimistic-desync with no reconciliation.
    FIX: Capture { error } from the update; on error show a toast/Alert and either reload or roll back the optimistic state. In AllReports, only apply the optimistic mutation after a successful write (or refetch on error).
- [medium] Staff status advance is optimistic with no error handling and no resolved_by/timestamp | src/screens/dashboard/StaffDashboardScreen.tsx:175
    PROBLEM: advanceStatus updates reports.status then unconditionally setReports(...) to the new status without checking { error }. A rejected write (RLS, offline) leaves the card showing the advanced status while the DB is unchanged; nothing reconciles until the next focus/refresh. There is also no rollback path.
    FIX: Await the update, check error, show a message and revert local state (or refetch) on failure; apply the optimistic change only after success.
- [medium] AllReports / ManageUsers / ManageStaff / ManageClubs / ManageFaculty do not refresh on focus | src/screens/dashboard/AllReportsScreen.tsx:72
    PROBLEM: These screens load data with useEffect(() => { load(); }, [load]) which runs once on mount only. When an admin assigns a report (AllReports), changes a role, creates a club, or returns from ReportDetail/ClubDetail where data changed, navigating back to an already-mounted screen shows stale data. AdminDashboard and StaffDashboard correctly use useFocusEffect; these five do not. Same pattern at ManageUsersScreen.tsx:54, ManageStaffScreen.tsx:59, ManageClubsScreen.tsx:72, ManageFacultyScreen.tsx:63.
    FIX: Replace the mount-only useEffect with useFocusEffect(useCallback(() => { load(); }, [load])) (as done in the two dashboards) so the list reflects changes made elsewhere.
- [low] Lists render arrays with .map inside ScrollView — no FlatList virtualization on large datasets | src/screens/dashboard/ManageUsersScreen.tsx:162
    PROBLEM: ManageUsers fetches up to 500 profiles (limit(500)) and renders them all via users.map inside a ScrollView with no virtualization, so every row mounts at once. AllReportsScreen.tsx:207 renders up to 200 reports the same way; ManageFacultyScreen renders filtered.slice(0,80) of 414 faculty. On lower-end devices this is a noticeable mount/scroll cost. Keys are stable (u.id), so correctness is fine, but it should be a FlatList for the larger lists.
    FIX: Convert the large lists (ManageUsers 500, AllReports 200) to FlatList with keyExtractor={u=>u.id} for virtualization; the faculty list is already capped at 80 so it is lower priority.
- [low] createUserAsAdmin updates role via the admin's live client but never refreshes the admin session token | src/services/adminService.ts:45
    PROBLEM: The role update uses the main `supabase` client (admin's session) which is correct, but the temporary signUp client (tmp) is created with autoRefreshToken:false and persistSession:false and signed out at line 50. That is fine. The real edge case: between signUp and the profiles.update, the code relies on a DB trigger having already created the profile row as 'student'. If the trigger is async/slow the .update().eq('id', data.user.id).select('id') can return 0 rows (updated.length !== 1) and the function reports 'assigning the role failed' even though the account was created — leaving an orphan student account with the wrong role and no retry.
    FIX: After signUp, upsert the profile row (insert-or-update) keyed on id instead of assuming the trigger already ran, or retry the update briefly; surface clearer guidance so the admin knows the account exists and only the role needs setting from the Users list.
- [low] ManageClubs fetches the entire club_members table on every load (unbounded, no club filter) | src/screens/dashboard/ManageClubsScreen.tsx:60
    PROBLEM: load() does supabase.from('club_members').select('club_id, role, profiles:user_id(full_name)') with no .in('club_id', ...) filter and no limit, then computes member_count and president_name client-side by filtering the full array per club. As membership grows this pulls every membership row in the system to the client on each refresh and does O(clubs x members) filtering.
    FIX: Compute counts/president server-side (an RPC or a view returning member_count and president per club), or at minimum filter club_members to the loaded club ids and add a sane limit.
- [medium] Faculty research_interests can be wiped to empty array when admin edits other fields | src/screens/dashboard/ManageFacultyScreen.tsx:98
    PROBLEM: On save, research_interests is set to interests.split(...) when the text is non-empty, otherwise to [] (empty array). The interests field is initialized from f.research_interests.join(', ') in openEdit, so normally it round-trips. But if a faculty row had interests stored and the admin clears the text box (or it was loaded as null and shows empty) and saves to change only email/phone, the update writes research_interests: [] — destroying existing data with no confirmation. Because there is no per-field dirty tracking, any save commits this field.
    FIX: Only include research_interests in the payload when the field was actually edited (track dirty state), or treat empty input as 'no change' (omit the key) rather than overwriting with [].
- [low] timeAgo shows negative/garbage for future or sub-minute timestamps | src/screens/dashboard/AdminDashboardScreen.tsx:41
    PROBLEM: timeAgo computes Math.floor(secs/60) with no floor at 0; for a just-created report (secs < 60) it renders '0m ago', and clock skew where created_at is slightly in the future yields negative values like '-1m ago'. Duplicated in StaffDashboardScreen.tsx:42.
    FIX: Clamp: const secs = Math.max(0, ...); and special-case <60s to 'just now'.
- [low] ManageUsers maintains two parallel toast mechanisms; role/expertise saves use the inline 'Saved' toast which can hide real outcomes | src/screens/dashboard/ManageUsersScreen.tsx:32
    PROBLEM: The screen has both a global showToast (useToast) and a local boolean `toast` state driving an inline 'Saved' pill. cycleRole/setExpertise optimistically mutate users state, then on DB error call showToast(error) AND await load() to revert — but the success path sets the inline toast. The optimistic update at line 76/88 changes the row before the await; if the update fails the row briefly showed the new role/trade. Minor visual desync, and having two toast systems is error-prone.
    FIX: Consolidate on the global useToast for success+error and apply the role/expertise change to local state only after a successful write (or keep optimistic but ensure load() fully reverts, which it does — so the main issue is the dual toast systems and the brief flash).
- [low] Unused useEffect import in both dashboard screens | src/screens/dashboard/AdminDashboardScreen.tsx:1
    PROBLEM: useEffect is imported but never used (only useState, useCallback, useFocusEffect are used). Same dead import at StaffDashboardScreen.tsx:1. Harmless at runtime but indicates copy-paste and will trip lint/no-unused-vars.
    FIX: Remove useEffect from the react import in both files.